# AutoApprove 검증 기록

검증일: 2026-09-21. 환경: macOS 26.4 arm64, Swift 6.3 Command Line Tools, Node.js 22.22.2. ad-hoc 서명을 사용하며 Developer ID 서명·Apple 공증은 포함하지 않는다.

## 0.2.12 Yes 변형·단축키·툴팁

- Claude Code·Codex 승인 화면과 Claude 단일 훅 질문에서 같은 허가의 반복 범위만 다른 3개 이상 선택지를 인식한다. 일반 Yes를 우선하며 `don't ask again`/`don’t`, 세션 내 허용, `y / a / esc`·`y / p / esc`, 괄호·대괄호, 추천 표시와 줄바꿈을 검사했다. 원래 답 라벨과 검증용 화면 원문을 보존하고 서로 다른 작업·모호한 Yes·다중 질문·다중 선택·다른 현재 선택·이전 화면은 자동 처리하지 않는다.
- 릴리스 빌드, 핵심 검사 **61/61**, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove`가 통과했다. 훅/소켓 검사에서 반복 허가가 첫 항목인 질문도 이번 요청의 원래 Yes 라벨로 답하고 중복 이벤트에 재응답하지 않았다. 실제 PTY 검사에 Codex의 줄바꿈된 `don’t ask again (a)`와 Claude의 `don't ask again`을 포함했다. 사용자 터미널에 시험 입력을 보내지 않았다.
- `$ui-design-workflow`를 적용해 기존 SwiftUI `.help`를 보완했다. 격리 앱의 실제 **1040×700**, **784×612** 관리 창에서 선택지 체크·직접 입력·전송 버튼 활성화·모의 응답 접수·질문 정리를 확인했다. 접근성 Help에서 체크 전후 문구, 빈 답변·연결 끊김·미설치·페이지 끝의 비활성 이유를 확인했다. **600×660** 연결 설정과 작은 승인 내역 창도 확인했다. [기본 창](dist/qa/tooltips-0.2.12-desktop.jpg), [작은 창](dist/qa/tooltips-0.2.12-compact.jpg), [설정](dist/qa/tooltips-0.2.12-settings.jpg), [내역](dist/qa/tooltips-0.2.12-history.jpg). 검증 질문은 종료 전에 정리했다.
- Computer Use에 hover 전용 동작이 없어 모든 툴팁 팝업의 실제 표시를 확인한 것은 아니다. 네이티브 툴바·메뉴는 접근성 트리에 Help가 노출되지 않아 코드 연결과 열기 동작까지만 확인했다. 전체 VoiceOver, 사용자 Terminal·알림 센터의 직접 입력은 검사하지 않았다. 브라우저·모바일 뷰포트는 네이티브 macOS 앱에 해당하지 않는다.
- 앱 **0.2.12 (15)**를 패키징·엄격한 코드 서명 검증 후 실행했다. 활성 **10개**, 자동 승인 **9개**, 동일 세션의 설정 변경 **0개**이며 사용자가 정한 순서·일시정지·Terminal 연결 설정을 보존했다. [실행 앱](dist/qa/tooltips-0.2.12-production.jpg). 내장 VS Code 확장은 **0.2.3**을 유지한다.
- 최종 DMG의 체크섬·읽기 전용 마운트·포함 앱 서명·Applications 링크·설치 안내를 검증하고 정상 추출했다. `AutoApprove-0.2.12-macOS-arm64.dmg`의 SHA-256은 `21d48261069940d79c6b747295e7274f5565c04014ca48e2578ad602929f234c`이다. DMG와 `.dmg.sha256`을 [v0.2.12 Release](https://github.com/newdlops/autoapprove/releases/tag/v0.2.12)의 배포 파일로 사용한다.

## 0.2.11 터미널 순서 저장·이동

- 기존 SwiftUI `List` / `ForEach.onMove`에 순서 저장을 연결하고 우클릭에 위·아래 이동을 추가했다. 현재 보이는 행의 ID와 순서를 확인한 뒤 해당 행이 차지한 자리만 재배치한다. 숨겨진 행·자동 승인·세션 ID·브랜치를 변경하지 않으며, 저장에 성공한 뒤 화면에 반영한다. 기존 설정 DB의 `sessionOrder`에 저장하고 앱 시작 시 복원한다.
- 릴리스 빌드, 핵심 검사 **58/58**, 훅/소켓 통합 검사와 실제 PTY 화면 통합 검사가 통과했다. 새 검사는 위·아래·맨 끝·여러 항목·제자리 이동, 필터의 숨은 위치 보존, 주기적 발견과 일부 세션부터 시작하는 재실행, 같은 폴더의 별도 세션, 새 세션·종료·폴더 변경, 오래되거나 잘못된 이동 요청, 손상·중복된 저장값과 저장 실패 시 원래 순서 보존을 포함한다.
- `$ui-design-workflow`를 적용했다. 생산 UI를 사용하는 격리 앱에서 우클릭 이동 후 선택 유지, 검색된 두 행만 이동한 뒤 숨은 행의 위치 보존, 맨 위 항목의 위로 이동 비활성, 스위치 독립 동작, 재배치된 행의 정확한 더블클릭 콜백을 확인했다. **1040×700**, **784×612**에서 안내 문구·선택 행·상세 배치를 확인했다. [기본 창](dist/qa/reorder-desktop.jpg), [작은 창](dist/qa/reorder-compact.jpg). 검증 질문은 종료 전에 정리했다.
- 직접 사용 확인: 0.2.11 실행 후 사용자가 터미널 행을 마우스로 드래그하면 순서가 바뀐다고 확인했다. 이는 사용자 확인이며 자동화 도구로 재현한 결과는 아니다. Computer Use에서는 생산 목록과 별도의 기본 `List.onMove` 예제 모두 드래그 재배치가 되지 않아, 자동화 검증은 같은 저장 경로를 사용하는 우클릭 이동으로 수행했다. 임시 입력 진단 코드와 기본 예제는 제거했다. 전체 VoiceOver와 실제 사용자 Terminal 열기 입력은 이번 범위에 포함하지 않았다.
- 앱 **0.2.11 (14)** 패키징·엄격한 코드 서명 검증 후 실행했다. 활성 **10개**, 자동 승인 **9개**, 동일 세션의 설정 변경 **0개**이며 기존 목록 순서·일시정지·Terminal 연결 설정을 유지했다. [실행 앱](dist/qa/reorder-production.jpg).
- 내장 VS Code 확장은 **0.2.3**이다. DMG 체크섬·읽기 전용 마운트·포함 앱 코드 서명·앱/Applications 링크/설치 안내 구성을 검사하고 정상 추출했다. `AutoApprove-0.2.11-macOS-arm64.dmg`의 SHA-256은 `d5d0772e20298499e5eb512e01bb490ebdff81733512e03d26ee849773299db8`이다. DMG와 `.dmg.sha256`을 [v0.2.11 Release](https://github.com/newdlops/autoapprove/releases/tag/v0.2.11)의 배포 파일로 사용한다.

## 0.2.10 현재 폴더의 Git 브랜치·DMG

- 목록의 터미널 제목 아래와 상세의 폴더 경로 아래에 현재 Git 브랜치를 추가했다. 긴 이름은 목록에서 가운데를 줄이고 상세에서 줄바꿈하며, 브랜치명·detached HEAD 커밋 검색과 전체 이름 툴팁을 제공한다. 최초 조회·비저장소·읽기 실패를 구분한다. 현재 폴더를 매번 다시 확인하고, 승인 처리와 독립된 배경 작업에서 최대 네 폴더씩 조회한다.
- 릴리스 빌드와 핵심 검사 **54/54**, 훅/소켓 통합 검사, PTY 화면 통합 검사가 통과했다. 임시 실제 Git 저장소에서 최초 커밋 전 브랜치·하위 폴더·연결된 워크트리·브랜치 전환·detached HEAD·비저장소·없는 폴더·상속된 Git 환경 격리를 확인했다. 폴더 변경·종료 후 늦은 결과를 무시하며, 검증용 PTY의 브랜치 전환이 수동 새로고침 없이 반영됨을 확인했다.
- `$ui-design-workflow`를 적용하고 생산 SwiftUI 화면을 사용하는 격리 앱에서 **1040×700**, **784×612**의 긴 이름·조회 중·실패·비저장소·detached HEAD를 확인했다. 브랜치 일부로 검색하면 해당 세션만 남고, 한 번 클릭은 선택만, 더블클릭은 정확한 세션의 모의 열기 콜백을 한 번 호출했다. [긴 브랜치 작은 창](dist/qa/git-branch-long-compact.jpg), [기본 창](dist/qa/git-branch-desktop.jpg), [조회 상태](dist/qa/git-branch-error-desktop.jpg). 검증 질문은 종료 전에 정리했다. 실제 Terminal 열기 입력과 전체 VoiceOver 검사는 이번 변경 범위에 포함하지 않았다.
- 앱 **0.2.10 (13)**를 패키징하고 엄격한 코드 서명 검증 후 실행했다. 실행 중인 **10개 세션**의 표시값을 각 폴더의 실제 Git 결과와 읽기 전용으로 대조해 불일치 **0개**를 확인했다. 기존 자동 승인 **9개**, 동일 세션 설정 변경 **0개**, 전체 일시정지 해제 상태를 보존했다. [실행 중인 앱](dist/qa/git-branch-production.jpg).
- 내장 VS Code 확장은 **0.2.3**이다. `node scripts/package-dmg.mjs`의 DMG 체크섬·읽기 전용 마운트·포함 앱 코드 서명 검증이 통과했으며 앱·Applications 바로가기·설치 안내를 확인하고 정상 추출했다. `AutoApprove-0.2.10-macOS-arm64.dmg`의 SHA-256은 `4d78e92f7b6f4143d86cd0a9641130511e0d48a3bb0b3e3cf5a17cfaeebbfaa3`이다. DMG와 `.dmg.sha256`을 [v0.2.10 Release](https://github.com/newdlops/autoapprove/releases/tag/v0.2.10)의 배포 파일로 사용한다.

## 0.2.9 DMG 배포

- 검증된 앱 **0.2.9 (12)**와 내장 VS Code 확장 **0.2.3**을 `AutoApprove-0.2.9-macOS-arm64.dmg`로 패키징했다. 이전 배포 0.2.7 이후의 완료 알림·Codex 기록 조회 복구·터미널 더블클릭을 함께 포함한다.
- `node scripts/package-dmg.mjs`의 `hdiutil verify`, 읽기 전용 마운트, 포함 앱의 엄격한 코드 서명 검증이 통과했다. 앱·Applications 바로가기·설치 안내 세 항목과 링크 대상·안내 원문 일치를 확인하고 정상 추출했다.
- SHA-256: `74fdd1d5b173b291c0dd2fdd05f49eeb54389fa0da139e5be6ebd0b9cafc0be9`. DMG와 `.dmg.sha256`을 [v0.2.9 Release](https://github.com/newdlops/autoapprove/releases/tag/v0.2.9)의 배포 파일로 사용한다. 소스는 같은 버전 태그로 고정한다. 아래 기능 검증은 해당 앱 빌드에서 수행했다.

## 0.2.9 Codex 기록 조회 복구·터미널 더블클릭

- 격리된 SQLite DB에 `BEGIN EXCLUSIVE` 잠금을 걸어 기존 질문 조회 오류와 ‘작업 완료 기록 형식을 지원하지 않습니다’가 함께 발생하는 것을 재현했다. 잠금을 풀면 같은 형식을 정상 조회했다. 실제 imgflash 세션의 스키마도 지원 형식이며 재확인 때 정상 조회됐다. 사용자 스크린샷 시점의 원시 SQLite 오류 코드는 확보하지 못했으므로 당시 원인이 반드시 잠금이었다고 단정하지 않는다.
- SQLite BUSY/LOCKED/SCHEMA, 기록 준비 중, 실제 테이블·열 누락, 파일 접근 실패와 잘못된 내용을 구분한다. 일시적 오류는 첫 두 조회까지 경고 없이 다시 확인하고 세 번 연속 실패하면 정확한 안내를 표시한다. 실패한 조회는 빈 목록과 구분해 기존 질문·질문 알림·증분 커서를 보존한다. 질문과 완료 조회는 각각 복구하며 확인되지 않은 완료 상태를 알림으로 보내지 않는다. 실제 스키마 오류는 즉시 표시하고 정상 조회가 되면 오류를 해제한다.
- 릴리스 빌드, 핵심 검사 **51/51**, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과. 새 검증은 실제 SQLite 잠금·해제, 잠금 중 새 질문 기록의 복구, 기존 질문 알림·관찰 시각 보존, 성공한 빈 대기열 반영, 실제 스키마 오류의 즉시 표시, 질문/완료 오류 분리, 읽기 전용으로 없는 DB를 생성하지 않는 동작을 포함한다.
- `$ui-design-workflow`를 적용하고 기존 SwiftUI 목록·선택·스위치·터미널 강조 경로를 유지했다. 생산 `SessionWindow`를 사용하는 격리 앱에서 한 번 클릭은 선택만, 더블클릭은 정확한 세션의 열기 콜백 1회, 다중 선택 중에는 클릭한 세션만 전달됨을 확인했다. 검색 후 대상 식별, 우클릭 열기, 자동 승인 스위치의 독립 동작, 연결 전 안내, **999×670** 및 **784×612**의 긴 제목·질문·버튼 배치와 작은 창 더블클릭을 확인했다. [기본 창](dist/qa/double-click-desktop.jpg), [작은 창](dist/qa/double-click-compact.jpg). 검증 질문은 종료 전 목록에서 정리했다.
- 앱 **0.2.9 (12)**를 패키징하고 `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과 후 실행했다. 재시작 직후 세 차례 상태 조회에서 활성 **9개**, 자동 승인 **8개**, Terminal 연결 **8개**, 기존 세션 설정 변경 **0개**, 전체 일시정지 꺼짐을 확인했다. Codex 두 세션의 질문·완료 오류는 없고 관찰 시각이 갱신됐다. 실제 앱의 imgflash 상세에서도 두 주황색 기록 오류가 사라진 것을 확인했다. [imgflash 상세](dist/qa/0.2.9-imgflash.jpg).
- 범위: 더블클릭의 UI 입력·정확한 세션 전달은 실제 네이티브 검증 앱에서 확인했지만, Computer Use가 Terminal 접근을 차단하므로 실제 사용자 Terminal 창 이동은 이번에 직접 실행하지 않았다. 기존 이동 코드의 정확한 TTY·최소화 복원·닫힌 탭 검사는 통과했다. 전체 VoiceOver 및 모든 macOS 버전은 별도 검증하지 않았으며, 웹·모바일 화면 검증은 해당하지 않는다.

## 0.2.8 최종 응답 완료 알림

- Claude 루트 `Stop`과 Codex 최신 루트 턴의 `completed`를 기존 macOS 알림 경로에 추가했다. 프로젝트·도구·최종 응답 요약을 표시하며 알림 클릭은 기존 세션 ID 검증·터미널 이동·강조를 사용한다. 자동 승인 설정·일시정지와 독립적이다. 3초 확인 중 새 작업이 관찰되면 전송을 취소한다.
- 최초 관찰의 과거 완료, 단순 idle, 도구 하나·하위 에이전트 종료, Codex failed/interrupted, 세션 종료는 완료 알림을 만들지 않는다. Claude 훅이 보고하는 백그라운드·예약 작업과 미응답 질문도 제외한다. Codex 완료 조회는 질문 수집과 오류를 분리하고 기존 읽기 전용 루트 매핑을 재사용한다. 실제 설치된 Codex 0.155.1의 `thread_turns` 상태·초 단위 시간·최종 응답 연결 스키마를 확인했다.
- `swift build --disable-sandbox --cache-path .build/cache -c release`, 핵심 검사 **48/48**, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과. 새 검증은 첫 기준선, 짧은 턴, 중복 Stop/완료, 작업 재개, 백그라운드·예약·질문 보존, 읽기 오류 복구, 최신 턴과 정확한 최종 응답 연결, 프로세스 종료 후 늦은 결과를 포함한다. helper→소켓→상태에서 실제 Stop JSON과 새 작업의 완료 상태 제거도 검사했다.
- 격리된 네이티브 앱에서 실제 `UNUserNotificationCenter` 전달 내역을 확인했다. 기존 질문 알림 2개와 공존하면서 초기 완료 **0개 → Claude 완료 1개 → Codex까지 완료 2개**, 같은 완료 반복 후 **2개 유지**, 새 작업과 완료 직후 0.5초 재개 후 **완료 0개·예약 0개**를 확인했다. 종료 전 검증 질문도 정리해 검증 앱 알림을 **0개**로 만들었다. [전달 결과](dist/qa/completion-delivered.jpg), [재개·취소 결과](dist/qa/completion-resumed.jpg). 검증용 이벤트만 사용했으며 사용자 대화에 시험 응답을 보내지 않았다.
- `$ui-design-workflow`를 적용해 기존 설정·폰트·상태색·툴팁을 유지했다. **600×660** 설정에서 새 안내 줄바꿈·허용 상태·시스템 설정 버튼과 아래 연결 항목까지 스크롤을 확인했다. 검증 창 캡처는 **1040×672**이다. [알림 설정](dist/qa/completion-settings.jpg). 전체 VoiceOver·키보드 흐름, 오류/권한 거부 상태의 새 화면은 이번에 별도로 시각 검증하지 않았다. 네이티브 앱이므로 웹·모바일 검증은 해당하지 않는다.
- 앱 **0.2.8 (11)** 패키징·`codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과 후 실행했다. 활성 **9개**, Terminal 연결 **8개**, 자동 승인 **8개**, 일시정지 해제와 기존 세션별 자동 승인 설정 일치를 확인했다. 실제 Codex 두 세션의 질문·완료 기록 읽기 오류는 없고 재실행 직후 과거 완료 알림 후보는 0개였다. 실제 앱 알림 권한은 **허용됨**이다.
- 이번 검증은 실제 사용자 TUI의 최종 응답→배너 클릭→터미널 이동 전체를 검증한 것은 아니다. Computer Use의 Terminal·알림 센터 접근 제한을 우회하지 않았다. Claude의 이전 버전에서 생략되는 백그라운드 정보나 다른 훅의 지연된 재개에는 감지 한계가 있다. 완료는 응답 종료이며 의미상 목표 달성이나 모든 명령의 성공을 보증하지 않는다.

## 0.2.7 DMG 배포

- release 빌드 스크립트로 앱 **0.2.7 (10)**과 내장 VS Code 확장 **0.2.3**을 생성했다. 앱·helper 모두 **arm64**, Mach-O의 최소 macOS 버전 **14.0**을 확인했다. 확장에는 `@xterm/headless 5.5.0`의 저작권·MIT 라이선스 안내를 포함했다.
- `node scripts/package-dmg.mjs`로 `AutoApprove-0.2.7-macOS-arm64.dmg`와 `.dmg.sha256`을 생성했다. 앱·Applications 바로가기·설치 안내만 포함하며 사용자 설정·승인 내역·검증 스크린샷은 포함하지 않는다.
- `hdiutil verify`, 읽기 전용 마운트, 마운트된 앱의 `codesign --verify --deep --strict` 통과. 이미지의 세 항목과 Applications 링크 대상, 설치 안내가 소스와 일치함을 확인하고 정상 추출했다. 자동화 스크립트 구문과 `git diff --check`도 통과했다.
- Developer ID 인증서가 없어 공증하지 않았다. 새로 다운로드한 파일의 Gatekeeper 허용 흐름·다른 Mac에서의 설치는 검증하지 않았으며 설치 안내와 Release에 서명 상태를 명시한다. Finder 창 배치 등 새 설치 UI는 추가하지 않았다.

## 0.2.7 앱 내 Codex 질문 응답

- 앱 **0.2.7 (10)** 패키징·엄격한 코드 서명 검증 후 재실행했다. 활성 9개·Terminal 연결 8개·자동 승인 8개·일시정지 해제와 각 활성 세션의 기존 자동 승인 설정을 보존했다. 최종 실제 앱 조회 시 두 Codex 세션은 수집 오류 없이 질문 0건으로 표시됐으며 입력 폼 검증은 아래 격리 검증 앱에서 수행했다. 사용자 대화에 시험 답변을 보내지는 않았다. [최종 앱](dist/qa/reply-production.jpg).

- Codex 비동기 질문에 선택지 체크박스·복수 선택·직접 답변·추가 설명·명시적인 ‘답변 보내기’를 제공한다. 답변이 비면 전송할 수 없고 질문별 입력 상태를 유지한다. ‘목록에서 정리’와 실제 답변 전송을 구분한다. Claude의 반환된 질문과 실행 권한 화면은 해당 요청 아래의 ‘터미널에서 답하기’로 기존 정확한 세션 이동·강조 경로를 사용한다.
- 전송 전 실제 프로세스 시작 시각·TTY와 유일한 루트 thread ID, 아직 남아 있는 정확한 질문을 다시 검증한다. 실행 중인 Codex 바이너리를 `proc_pidpath`로 찾고 해당 CODEX_HOME에서 `queue --thread UUID --message TEXT`를 직접 인자 배열로 실행한다. 셸 해석 없이 질문 인용·선택지 원문·직접 입력을 전달한다. 공유 daemon 시작이나 사용자 터미널 시험 입력은 없다.
- 접수된 답변은 **전달 대기**이며 현재 턴이 실제 수신했다는 주장은 하지 않는다. 사전 검증 실패는 재시도할 수 있고, 실행 후 타임아웃·불명확한 접수 결과는 확인 필요로 남겨 중복 전송을 차단한다. 전송 전에 감사 기록과 지속 예약 상태를 저장한다. 전송 중 앱 종료 또는 재실행에도 무조건 다시 보내지 않는다. 자동 승인 설정과 별개의 명시적 수동 응답이다.
- `swift build --disable-sandbox --cache-path .build/cache -c release`, **43/43** 핵심 검사, 기존 훅/소켓 통합 검사와 PTY 화면 통합 검사 통과. 추가 범위: 복수 선택·직접 입력의 정확한 조합, 여러 줄·셸 특수 문자·빈 값·NUL, 다른 thread 접수 거부, 전송 전 기록·예약, 같은 질문 재전송 및 재시작 차단, 결과 미확인과 중간 종료 복원, 잘못된 대화/감사 기록 실패 시 입력 미전달, 전달 대기 기록 필터.
- `node scripts/codex-queue-integration-check.mjs` 통과. 임시 CODEX_HOME의 합성 세션에 설치된 **Codex 0.155.1** CLI로 메시지를 등록하고 공식 `thread/queue/list`에서 정확한 큐 ID·대화 ID·여러 줄 답변과 셸 특수 문자 보존을 확인했다. 모델 턴·외부 API 호출 없이 수행하며 기존 사용자 대화에 답변을 보내지 않는다. 실제 사용자의 실행 중인 TUI가 이 답변을 소비하는 시점은 검증하지 않았다.
- 생산 엔진과 UI를 사용하는 격리 검증 앱에서 **902×612**의 복수 체크박스 선택·추가 한글 답변 입력·빈 답변 버튼 비활성·전송 클릭, **1040×700**의 접수 결과와 남은 질문 건수, **784×612**의 긴 질문·결과 미확인 안내를 확인했다. 검증 앱의 전송 경로는 성공/불명확 응답을 주는 모의 수신자이며 사용자 터미널을 열지 않는다. [선택과 직접 입력](dist/qa/reply-choices.jpg), [접수 상태](dist/qa/reply-sent-desktop.jpg), [전송 결과 기록](dist/qa/reply-history.jpg). 접근성 트리에서 체크박스·전송 버튼 상태, 실패 후 전송 버튼 제거와 직접 입력한 답변 보존, 저장된 ‘답변 대기열 등록’·‘답변 접수 확인 필요’ 항목을 확인했다. 전체 키보드/VoiceOver 및 실제 Terminal 이동은 이번 검증 범위 밖이다.

## 0.2.6 Codex 질문 대기열·터미널 제목·승인 상세 접기

- 설치된 Codex **0.155.1**의 app-server JSON schema, 로컬 이력 DB 스키마와 질문/답변 이벤트 형식을 확인했다. 기존 CLI에는 연결 가능한 공유 daemon 소켓이 없었다. `queue_1.sqlite`의 사용자 입력 큐와 비동기 질문은 별개다. 실제 프로세스가 열어 둔 유일한 `source: cli` 루트 rollout으로 `thread_history_1.sqlite`를 연결하고 읽기 전용·일관된 트랜잭션·증분 커서로 수집한다. 하위 에이전트·동일 프로젝트의 다른 프로세스·모호한 루트는 섞지 않는다. Codex 설정이나 기록을 수정하지 않는다.
- 질문 대기열은 작업 상태와 독립적이며, 여러 질문의 ID·선택지를 유지한다. 정확한 질문 인용과 답변을 유일하게 연결한 항목만 해제하고 일반 입력·도구 완료로 없애지 않는다. 미응답 상태 API를 읽는 기능은 아니다. 인용 없는 답변·취소·같은 제목의 중복 질문은 남을 수 있어 로컬 ‘확인 완료’를 제공하고 재실행 후에도 유지한다. 읽기 실패에는 마지막 목록을 보존한다. 목록의 건수·확인 필요 필터·메뉴 막대·알림 추적을 다중 질문에 적용했다.
- 프로젝트명 다음 줄에 Terminal 사용자 지정 탭 제목 또는 해당 선택 탭의 창 제목, VS Code 등록 이름을 표시한다. 제목 검색·전체 제목 툴팁을 제공한다. 선택되지 않은 탭에는 다른 탭의 창 제목을 붙이지 않는다. 선택적 제목 조회 실패는 화면 읽기나 승인을 막지 않는다. 기존 VS Code 확장 0.2.3의 이름 전달을 사용하며 확장 변경은 없다.
- 최근 승인 기록에 네이티브 DisclosureGroup을 적용했다. 결과 문구/화살표로 전체 요청·선택한 답·출처를 펼치고 다시 접으며 각 항목은 독립적이다. 마우스로 결과 문구를 눌러 펼침/접힘과 여러 항목 동시 펼침을 확인했다. 접근성 트리의 disclosure triangle on/off도 확인했다. 키보드만으로 완주하는 흐름과 VoiceOver는 이번 검증에 포함하지 않았다.
- Release 빌드와 **39/39** 핵심 검사, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과. 추가 검사: 질문별/부분 답변·일반 메시지·인용만 있는 입력·모호한 동일 제목, 읽기 전용 DB·증분 갱신·재실행·미지원 스키마, 정확한 루트 매핑, 동시 알림·화면 갱신·일시 오류·로컬 정리 유지·종료 후 늦은 결과, Terminal 제목 계약과 VS Code 이름 갱신. 사용자 터미널에 시험 입력을 보내지 않았다.
- `$ui-design-workflow`와 Impeccable의 기존 UI 보존·마무리 지침을 적용했다. 격리된 네이티브 검증 앱에서 **1040×700**, **784×612**의 긴 제목, 두 질문/긴 선택지, 질문 한 건 정리 후 다른 건 유지, 제목 검색·검색 결과 없음·확인 필요 필터, 40줄 전체 요청·답변 펼침과 스크롤을 확인했다. [기본 질문 목록](dist/qa/queue-desktop.jpg), [최소 창 질문 목록](dist/qa/queue-minimum.jpg), [승인 상세](dist/qa/approval-expanded.jpg), [최소 창 승인 상세](dist/qa/approval-expanded-minimum.jpg). 선택 행에서 질문 수가 잘 보이도록 시스템 전경색을 사용했다. 브라우저·모바일 검증은 네이티브 macOS 앱에 해당하지 않는다. 전체 Impeccable 독립 이중 평가나 웹 감사는 수행하지 않았다.
- 앱 **0.2.6 (9)** 패키징 및 `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과 후 실행했다. 실제 활성 **9개**, Terminal **8개 연결**, 자동 승인 **8개**와 일시정지 해제 상태를 확인했다. 살아 있는 동일 세션의 기존 자동 승인 설정은 모두 보존됐으며 검증 PTY의 종료된 임시 행 3개는 재실행 목록에서 사라졌다. 실제 **8개 터미널 제목**, Codex 두 루트 세션의 기록상 미확인 질문 **2건/12건** 수집을 확인했다. 이는 기록 기반 미확인 건수이며 실제 TUI 큐와 일대일 대조한 수치는 아니다. [최종 앱](dist/qa/queue-production.jpg).
- 사용자가 허용한 알림 권한은 최종 앱 설정에서 **허용됨**으로 확인됐다. [최종 알림 설정](dist/qa/queue-notifications-allowed.jpg). 이번에도 실제 Terminal/알림 센터의 클릭→이동 검증은 Computer Use 접근 제한으로 수행하지 않았다. 질문별 알림 추적 로직과 기존 클릭 시 세션 검증·강조 경로는 유지한다.

## 0.2.5 예·아니오 판별 확장·응답 알림

- Claude 훅 질문 판별을 질문 문구 대신 명확한 두 개의 예·아니오 선택지로 변경했다. 사용자 제공 ‘미커밋·브랜치 현황을 먼자 훑어볼까요?’를 실제 helper→소켓→응답 JSON으로 검사했다. 순서가 바뀐 선택지, 추천 표시, 쉼표·대시 뒤 설명, 한국어 정규화를 지원하고 원래 답 라벨을 보존한다. 세 가지 작업 선택, 다중 질문·다중 선택, 기존 답, 모호한 선택지는 자동으로 답하지 않는다.
- 질문 후 Claude의 `permission_prompt`·`idle_prompt`가 와도 미응답 질문과 선택지를 유지한다. 자동 처리할 수 없는 질문·수동 확인 요청에는 프로젝트·도구·질문을 담은 macOS 알림을 추가했다. 반복 관찰과 같은 도구 요청은 중복 알림을 막고 해결·종료 시 정리한다. 자동 처리 중인 권한 요청과 단순 입력창 타이핑은 제외한다.
- 알림 클릭과 ‘터미널 열기’는 동일한 터미널 이동·강조 코드를 사용한다. 클릭 시 세션 ID를 확인하고 살아 있는 PID·시작 시각·TTY·도구를 다시 검사한다. 같은 프로젝트·재사용된 TTY를 다른 세션의 이동 대상으로 쓰지 않는다. 앱이 종료된 상태에서 알림으로 실행되면 최초 탐색 완료를 기다린다. 연결 없음·종료·권한 오류는 오류 창으로 알린다.
- `swift build --disable-sandbox --cache-path .build/cache -c release`, `.build/release/autoapprove-checks` **34/34**, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과. 알림 회귀 범위는 삼지선다, 반복 관찰, 질문 해결, 같은 내용의 새 질문, 다른 tool_use_id, 일시정지·자동 승인 끄기, 입력 전달 실패, 잘못된 이동 대상·종료·연결 없는 VS Code이다. 사용자 터미널에 시험 입력을 보내지 않았다.
- 격리된 검증 앱에서 생산 `QuestionNotifications`를 사용해 macOS에 실제 알림이 전달된 것(1개), 같은 질문 재관찰 후 1개 유지, 질문 해결 후 0개 제거를 시스템 API로 확인했다. 검증 창의 수신 카운터에 **알림 클릭 1회**도 관측했다. 알림 클릭 순간의 배너와 강조 표시를 연속으로 시각 관찰한 것은 아니며, 검증 앱의 이동 대상은 자체 창이다. 실제 사용자 Terminal/VS Code로의 알림 이동은 검증하지 못했다. Computer Use가 `com.apple.UserNotificationCenter`와 Terminal 접근을 제한하며 이를 다른 도구로 우회하지 않았다.
- `$ui-design-workflow`를 적용해 기존 SwiftUI 구성·시스템 글꼴·SF Symbols·의미 색상을 유지했다. 실제 앱 **1226×722**, **784×612**에서 설명의 줄바꿈·긴 기존 질문·목록/상세 스크롤을 확인했다. [기본 창](dist/qa/notifications-desktop.jpg), [최소 창](dist/qa/notifications-minimum.jpg). **600×660** 설정에서 알림 권한 대기·비활성 허용 버튼·도움말·기존 연결 항목까지의 스크롤을 확인했다. [알림 설정](dist/qa/notifications-settings.jpg), [설정 아래쪽](dist/qa/notifications-settings-bottom.jpg). 네이티브 macOS 앱이므로 웹 뷰포트·React 감사는 해당하지 않는다. 최종 재실행 후 macOS 권한이 비허용 상태로 확인되어 **784×612**에서 [알림 꺼짐 안내](dist/qa/notifications-disabled.jpg)와 [최종 설정](dist/qa/notifications-settings-final.jpg)도 확인했다. 전송 오류 UI·VoiceOver 전체 흐름은 시각 검증하지 않았다.
- 앱 **0.2.5 (8)** 패키징과 `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과. 재실행 후 활성 9개 세션, Terminal 8개 연결, 기존 자동 승인 8개와 전체 일시정지 해제 설정 보존을 확인했다. 최종 알림 권한은 macOS의 비허용 상태로 확인됐다. 시스템 알림 설정에서 AutoApprove 알림 허용을 직접 켜야 하며 앱의 해당 안내 화면을 열어두었다. 권한 설정은 도구로 변경하지 않았다. VS Code 확장은 변경하지 않았고 기존 0.2.3 번들을 유지한다.

## 0.2.4 앱 아이콘·툴팁·Claude 작업 시작 응답

- 내장 이미지 생성 도구로 남색 터미널·흰 프롬프트·민트 체크 아이콘을 만들었다. [원본 PNG](assets/AppIcon.png)와 [생성 프롬프트](assets/README.md)를 저장했다. 원본은 1254×1254 RGBA이며 패키징 과정에서 16–1024px의 표준 macOS 표현과 ICNS를 만든다. 앱 정보 창에서 **0.2.4 (7)** 버전과 실제 번들 아이콘을 확인했다. [앱 정보 창](dist/qa/icon-about.jpg). 64px 파생 이미지의 기호 구분도 육안으로 확인했다. `iconutil`은 샌드박스 안에서 Invalid Iconset을 반환했으나 정상 실행 환경에서 변환됐으며 아이콘을 포함한 최종 코드 서명 검증을 통과했다.
- 관리 창, 연결 설정, 승인 내역, 메뉴에 기본 `.help` 툴팁을 추가했다. 검색·필터·자동 승인·작업 상태·TTY/PID·터미널 이동·결과의 의미를 설명하고 연결 전·연결 중·일시정지·페이지 끝·복사 완료에 따라 문구를 바꾼다. 중요한 자동 응답 범위와 오류 안내는 본문에도 유지한다. 사용자 스위치나 연결 설정을 시험 목적으로 바꾸지 않았다.
- 실제 앱 **1040×700**, **784×612**에서 검색, 세션 상세, 새 자동 응답 설명의 줄바꿈, 연결 설정 열기·닫기와 도움말 연결을 확인했다. [기본 창](dist/qa/tooltips-desktop.jpg), [최소 창](dist/qa/tooltips-minimum.jpg), [연결 설정](dist/qa/tooltips-settings.jpg). 접근성 트리에서 검색·필터·스위치·연결 버튼·비활성 페이지 버튼의 Help 내용을 확인했다. Computer Use에 hover 전용 동작이 없어 모든 툴팁 팝업의 실제 표시를 시각 검증한 것은 아니다. VoiceOver 전체 흐름도 검증하지 않았다.
- 사용자가 지목한 `vcmsecond` 요청은 `AskUserQuestion`의 예·아니오 작업 시작 확인이었다. 이전 구현은 이 도구를 항상 직접 응답 대상으로 남겼다. 단일 예·아니오 작업 시작/계속 확인을 인식해 선택지 위치와 무관하게 ‘예’ 라벨을 `updatedInput.answers`로 전달하도록 추가했다. 원래 질문·선택지와 선택한 답을 기록하며 다중 질문·다중 선택·다른 대안 선택·기존 답을 변경하지 않는다. [공식 훅 응답 형식](https://code.claude.com/docs/en/hooks#pretooluse-decision-control)을 확인했고 설치된 Claude CLI 버전은 **2.1.92**였다. 이미 반환된 질문을 기록으로 재생하거나 사용자 터미널에 테스트 입력을 보내지 않았다.
- Release 빌드 및 핵심 검사 **31/31**, 훅/소켓 통합 검사, 테스트 PTY 통합 검사 통과. 추가 범위: 한국어 정규화, 순서가 바뀐 예/아니오, 정확한 선택지 문자열과 원래 질문 보존, 무관한 질문 제외, PreToolUse/PermissionRequest의 응답 위치, 같은 tool_use_id 중복 방지, 세션별 켜짐·전체 일시정지·세션 격리, 저장 실패 시 응답 차단, 답의 영구 기록과 결과 필터. 실제 helper stdin→소켓→JSON stdout으로 질문 응답을 왕복 검사했다. 새 질문에 대한 실제 Claude UI의 수락·작업 재개는 아직 검증하지 않았다.
- 별도 검증 앱의 합성 질문에서 검색→상세 선택, `선택한 답: 예`, 질문 내용·프로젝트·결과를 **784×612 라이트·다크**로 확인했다. [질문 응답 내역](dist/qa/start-answer-audit.jpg), [다크 모드](dist/qa/start-answer-audit-dark.jpg). 검증 기록은 사용자 데이터베이스에 넣지 않았다.
- 최종 앱 재실행 후 Terminal **8개 연결**, 활성 Claude/Codex **9개**, 재실행 전 켜져 있던 자동 승인 **8개 설정**, 일시정지 해제와 기존 기록 **14건** 보존을 확인했다. Claude의 새 작업 이벤트도 수신했다. 사용자가 QA 도중 창 크기와 선택을 바꾼 후에는 해당 사용자 상태를 유지했다. VS Code 확장은 이번 변경 대상이 아니며 번들에는 기존 **0.2.3** 확장을 포함했다.

## 0.2.3 터미널 강조·승인 내역

- `터미널 열기`는 정확한 TTY의 탭을 선택하고 최소화된 창을 복원한 뒤, 해당 창 위치에 3초 동안 파란 테두리와 프로젝트·도구·TTY를 표시한다. 강조 패널은 키보드 포커스를 받지 않고 마우스 입력을 통과시키도록 구현했다. VS Code 확장은 대상 터미널을 연 뒤 이름이 포함된 알림과 3초 상태 표시를 제공한다.
- 전체 `승인 내역` 창을 추가했다. 세션 종료·앱 재실행 이후에도 요청 내용, 시각, 프로젝트 경로, 도구, PID·TTY, 전달 결과를 조회한다. 검색, 결과 필터, 100건 단위 페이지, 요청 복사를 제공한다. SQLite의 기존 2,000건 자동 삭제 제한을 제거했으며 기존 기록의 누락된 프로젝트 정보를 임의로 복원하지 않는다.
- 화면 승인 시도를 입력 전에 저장하고 같은 기록의 결과를 갱신한다. 저장 실패 시 입력을 보내지 않고 앱에 오류를 표시한다. 연결이 끊기거나 입력 결과가 불명확하면 성공으로 표시하지 않는다. `승인 전달`은 응답 전달 기록이며 명령 실행 성공을 뜻하지 않는다.
- Release 빌드와 핵심 검사 **27/27**, VS Code TypeScript·화면 모듈 검사 **5/5**, 훅/소켓 통합 검사, 테스트 PTY 통합 검사를 통과했다. 추가 검증은 2,006건 보존·재열기, 페이지 중복 방지, 검색 이스케이프, 결과 필터, 이전 JSON 호환, 긴 훅 입력과 세션 종료 후 메타데이터 보존, 시도·응답의 단일 기록 갱신이다. Terminal 이동은 JavaScriptCore 모의 객체로 정확한 탭·최소화 복원·닫힌 탭·권한 거부를 검사했고, 다중 화면 좌표 변환을 단위 검사했다.
- 별도 검증 앱에 합성 기록 125건을 넣어 실제 네이티브 렌더링을 확인했다. **1040×700**에서 긴 요청, 2페이지 이동과 선택, 검색 결과 없음, **784×612**에서 줄바꿈과 결과 필터를 확인했다. 필터는 42건, 프로젝트 검색과 조합하면 21건을 반환했다. 페이지 이동 시 이전 스크롤 위치가 남는 문제를 수정하고 맨 위에서 새 페이지가 시작되는 것을 재확인했다. [기본 창](dist/qa/audit-desktop.jpg), [2페이지](dist/qa/audit-page-two.jpg), [작은 창·필터](dist/qa/audit-minimum-filter.jpg), [검색 결과 없음](dist/qa/audit-empty-search.jpg).
- 같은 생산 코드의 강조 패널을 별도 **480×332** 검증 창에 띄워 파란 테두리, 긴 프로젝트명 말줄임, TTY 배지와 시간 경과 후 해제를 확인했다. [강조 패널](dist/qa/highlight-panel.jpg). 검증 앱은 사용자 데이터베이스·터미널을 읽거나 승인 입력을 보내지 않는다. 실제 Terminal 창 이동과 강조의 연속 동작, 실제 VS Code 알림, 다크 모드·VoiceOver·요청 복사는 이번 UI 검증에 포함하지 않았다. Computer Use의 Terminal 접근 제한을 우회하지 않았다.
- 앱·확장을 **0.2.3**, 앱 빌드 **6**으로 패키징했고 `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app`를 통과했다. 실행 중인 앱을 교체한 후 Terminal **8개 연결**, 활성 Claude/Codex **9개**, 일시정지 해제, 기존 승인 기록 **5건**을 확인했다. 전체 승인 내역 화면을 열어 기존 기록의 내용과 이전 버전 메타데이터 누락 안내를 확인했다. [실제 승인 내역](dist/qa/audit-production.jpg).
- 재실행 전 켜져 있던 자동 승인 6개는 유지됐다. 재실행 후에는 7개가 켜져 있으며, 추가 1개는 데이터베이스에 이미 저장되어 있던 `true` 설정이 복원된 것을 읽기 전용 조회로 확인했다. 이번 작업에서 사용자 승인 스위치를 변경하지 않았다. VS Code 확장은 패키지만 생성했으며 사용자 VS Code에는 설치하지 않았다.

## 0.2.2 요청 감지·승인 전달 수정

- 실제 앱의 상태 API에서 Terminal 연결이 꺼져 있는데 자동 승인 설정은 유지된 상태를 확인했다. 이전 버전은 Terminal 연결 설정을 저장하지 않았다. 실제 Codex 승인 내역에는 승인 직전 전체 화면 비교가 실패한 `승인 화면이 변경되었습니다. (-2700)`가 남아 있었다. 실패 시 재시도하지 않는 엔진 동작을 확인했다. 해당 실제 화면의 정확한 변경 원인이 한글 정규화인지 로그 변경인지는 확인하지 못했다.
- 명시적으로 켠 Terminal 연결을 저장하고 재실행 후 복원한다. 연결 해제는 유지하고, 일반 연결 오류는 5초 이후 재시도하며, macOS 자동화 권한 거부는 사용자의 재연결 전까지 반복 호출하지 않는다. 이전 버전에서 저장되지 않은 연결은 임의로 켜지 않는다.
- 긴 명령의 승인 창과 한국어 NFC/NFD 차이를 처리한다. Codex는 현재 요청 전체를 비교하여 위쪽 로그 변화에 영향을 받지 않도록 했으며, Claude는 승인 제목 이전의 명령도 비교한다. 선택지·명령·뒤에 추가된 입력창 변경은 입력을 차단한다.
- 입력이 전혀 전달되지 않았다는 어댑터 응답이 있을 때만 다음 화면 수신 후 최대 3회 재시도한다. 성공했거나 전송 여부를 알 수 없는 입력은 반복하지 않는다. 연속 요청 사이의 화면 변화를 관찰하지 못하면 다음 요청도 직접 확인이 필요할 수 있다.
- Claude `AskUserQuestion`과 계획 확인의 내용을 보존하고, 지원하는 선택형 질문을 두 도구 모두 `응답 필요`로 표시한다. 일반 질문에는 자동으로 답을 고르지 않는다. 훅에서 터미널로 반환된 권한 요청은 지원하는 화면 연결이 있을 때 처리하며 이미 훅으로 승인한 요청에 화면 입력을 중복 전송하지 않는다.
- Release 빌드 및 Swift 검사 **23/23**, VS Code TypeScript·화면 모듈 **5/5**, 훅/소켓 통합 검사, 테스트 PTY 통합 검사 통과. 추가 범위: 연결 복원·해제·권한 거부, 긴 명령, 한국어 정규화, 실제 JXA 스크립트의 메모리 모의 실행, 질문 내용·분류, 입력 미전달 재시도와 횟수 제한, 훅 권한 요청의 화면 전환. 사용자 터미널에 테스트 입력을 보내지 않았다.
- 앱과 확장을 **0.2.2**로 패키징했다. `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과. 수정본 재실행 후 실제 활성 목록에서 Claude·Codex 9개와 일반 셸 0개를 확인했고 기존 자동 승인 6개 설정이 모두 보존됐다. Claude의 실제 작업 이벤트 수신도 확인했다.
- 기능 테스트와 별도로 실제 앱의 **1040×700**, **784×612**에서 연결 안내·행 상태·선택 전환을 확인하고 연결 설정 시트를 열어 복원 설명과 버튼 상태를 확인했다. 기본 크기로 복원했다. [기본 창](dist/qa/request-fix-desktop.jpg), [최소 창](dist/qa/request-fix-minimum.jpg), [연결 설정](dist/qa/request-fix-settings.jpg). 다크 모드·VoiceOver와 실제 질문 내용의 UI 렌더링은 이번 검증에 포함하지 않았다.
- 사용자가 수정본에서 Terminal 연결을 켠 뒤 로컬 상태 API로 `연결됨 · 8개 세션`, `terminalRequested: true`, `terminalConnected: true`를 확인했다. 전체 활성 목록 9개 중 1개는 Claude의 별도 백그라운드 PTY다. 기존 자동 승인 6개와 전체 일시정지 해제 상태도 유지됐다. 다만 새 승인 기록은 없고 여러 화면 연결 세션이 `unknown`이어서 실제 질문 감지→입력→작업 재개는 아직 확인하지 못했다. 사용자에게 현재 질문·선택지 문구를 요청했다.
- Computer Use 도구의 Terminal 접근 제한은 우회하지 않았다. 실제 Claude/Codex 승인 성공을 테스트 결과나 연결 성공만으로 주장하지 않는다. VS Code 확장 패키지는 생성했지만 사용자 VS Code에 새 버전을 설치하거나 실환경 입력을 검증하지 않았다.

## 0.2.1 Claude Code·Codex만 표시

- 사용자 요구에 맞춰 프로세스 탐색과 엔진 등록을 Claude Code·Codex로 제한했다. 일반 셸과 다른 명령은 목록 및 상태 개수에 포함하지 않는다. 도구 종료 후 셸이 대신 나타나지 않는다. 기존 CLI 입력 대기 감지와 경과 시간은 유지한다.
- 일반 셸 제외, 두 도구의 Terminal/VS Code 탐색, 도구 종료 시 활성 목록 및 대기 수 제거 회귀 검증을 갱신했다. `swift build --disable-sandbox --cache-path .build/cache -c release`, `.build/release/autoapprove-checks` **18/18**, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과.
- 앱 0.2.1 실행 후 실제 로컬 상태 API에서 Claude 6개·Codex 2개, 일반 셸 0개를 확인했다. 이전 목록의 일반 셸 34개가 제외됐다. 사용자가 자동 승인을 켠 기존 6개 세션의 설정도 보존됐다.
- 실제 앱의 1040×700 목록과 784×612 대기 필터를 확인했다. 일반 셸이 대기 개수에 섞이지 않고 빈 대기 필터에는 Claude Code·Codex의 입력 대기 설명이 표시된다. 기본 창 크기를 복원했다. 캡처: [에이전트 목록](dist/qa/agents-only-desktop.jpg), [작은 창 대기 필터](dist/qa/agents-only-idle-minimum.jpg).
- 최종 앱의 `codesign --verify --deep --strict` 통과. Terminal 화면 연결은 이전부터 해제된 상태였으며 자동으로 연결하지 않았다. CLI의 화면 기반 대기 감지는 기존 fixture/모의 확장 검증 범위이고, 이번 변경으로 실환경 화면 판정 정확도를 새로 검증한 것은 아니다.

아래 0.2.0의 일반 셸 지원 기록은 이전 동작에 대한 기록이며, 현재 버전에는 적용하지 않는다.

## 0.2.0 대기 터미널 표시

- Claude·Codex 외에 macOS Terminal/VS Code의 일반 셸을 탐색한다. 같은 TTY의 에이전트와 셸은 중복 표시하지 않으며 에이전트 종료 후 셸이 다시 나타난다. 작업 디렉터리 조회는 PID를 모아 한 번에 수행하고 셸의 `cd` 변경을 반영한다.
- 실제 프로세스 탐색에서 42개 활성 세션을 확인했다: 에이전트 8개, 일반 셸 34개. 이 중 일반 셸 30개는 대기, 4개는 명령 실행 중으로 판정됐다. 프로세스 탐색은 Terminal 화면에 접근하거나 입력을 보내지 않았다.
- 일반 셸의 포그라운드/자식 명령, Claude의 Stop·idle_prompt·UserPromptSubmit 이벤트, 지원하는 CLI 입력창·중단 안내를 사용한다. 화면 대기는 동일한 화면과 연결 세대를 2초 이상 재확인한다. 조용한 작업, 백그라운드 자식 명령, 알 수 없는 화면, 연결 해제는 대기로 오인하지 않는 fixture를 검증했다. 일반 셸은 승인 감지·전송에서 제외했다.
- 핵심 검사 **18/18 통과**. 일반 셸/명령/백그라운드 작업, 에이전트 종료 후 셸 복귀, 입력창/작업/질문/미확인 분류, 화면 세대 변경, 대기 시간 유지·초기화, 훅 이벤트 전환, 재실행 직후 훅이 먼저 도착하는 설정 보존 회귀를 추가했다. 최종 release 검사도 통과했다.
- VS Code TypeScript 빌드 및 화면 모듈 **3/3 통과**. 확장은 관찰한 셸 명령 실행 여부를 등록 메시지에 추가한다. 이미 실행 중인 명령의 과거 출력을 복구하지 않는다. 실제 VS Code 확장 UI의 명령 이벤트 왕복은 이번 검증 범위에 포함하지 않았다.
- 훅/소켓 통합 검사 통과. 테스트 PTY와 모의 확장을 사용하는 화면 통합 검사에서 입력 대기 1회 관찰은 미확인, 반복 관찰 후 대기, 새 작업 시 작업 중 및 대기 시각 제거, 대기 감지 중 승인 입력 없음도 확인했다. 사용자 터미널에는 테스트 입력을 보내지 않았다.
- 실제 앱에서 1040×700, 784×700, 최소 784×612를 확인했다. 42개 밀집 목록, 대기 필터, 대기 감지 후 경과 시간, TTY 검색, 긴 프로젝트 이름, 검색 결과 없음, 검색 지우기, 목록 스크롤, 일반 셸의 승인 스위치 제외, 연결 전 VS Code 이동 버튼 비활성을 확인했다. 기본 창 크기로 복원했다. 캡처: [최소 창 대기 목록](dist/qa/idle-minimum.jpg), [최종 기본 창 목록](dist/qa/idle-desktop.jpg).
- UI QA 중 재실행 직후 훅이 먼저 도착하면 저장된 자동 승인 설정을 복원하지 못하는 문제를 발견해 수정했다. 최종 앱의 로컬 상태 API에서 사용자가 켠 기존 6개 세션 모두 `automatic: true`가 유지됨을 확인했다.
- 앱 및 확장 패키지 0.2.0을 생성했다. 최종 앱의 `codesign --verify --deep --strict` 통과. 기존 Terminal 직접 접근 제한은 유지되며 우회하지 않았다. 앱 재실행 후 Terminal 화면 연결은 사용자가 연결 설정에서 다시 켜야 한다. Claude 훅은 실제 작업 이벤트 수신을 확인했다. Claude·Codex 화면 기반 대기 분류는 fixture/모의 확장으로 검증했으며, 사용자 Terminal의 실제 화면 판정 정확도를 검증했다고 주장하지 않는다.

대기 판정의 한계: 셸 내부 내장 명령, 사용자 정의 CLI 화면, 완료 훅 이후 다른 훅이 이어가는 작업은 추론의 한계가 있다. 대기 시간은 감지 이후부터 계산한다. 앱 재시작 전 대기 시간은 복원하지 않는다. 다크 모드와 VoiceOver 전체 흐름은 이번에도 확인하지 않았다.

## 0.1.1 연결·누락 수정

- 사용자 환경에서 앱 상태의 `Terminal 연결 실패 … (-1728)`를 확인했다. 설치된 `Terminal.sdef`에는 탭의 `name` 속성이 없다. 사용하지 않던 제목 조회를 제거하고 `tty`·`contents`만 읽도록 수정했다.
- `ps`의 마지막 열 앞 정렬 공백이 남아, 절대 경로 없이 `claude`·`codex`로 표시된 실행 파일을 인식하지 못했다. 마지막 열의 정렬 공백을 제거했다. 실제 프로세스 비교에서 누락됐던 Claude 4개가 복구됐고 수정 직후 8개 CLI 세션을 탐색했다.
- 조상 Terminal과 다른 PTY에서 실행되는 백그라운드 에이전트를 같은 Terminal 탭으로 잘못 분류하지 않도록 수정했다. 해당 프로세스는 목록에 남기되 터미널 종류는 미확인으로 표시한다.
- 닫힌 창·탭은 개별 실패로 기록해 다른 탭을 계속 읽고, 읽기에 실패한 세션의 기존 화면 연결·예약 입력은 폐기한다. 권한 거부는 성공으로 숨기지 않는다.
- 새 회귀 검증 3개를 포함한 핵심 검사 11/11, 기존 훅·소켓 및 PTY 통합 검사 통과. Terminal JavaScript 자동 검증은 JavaScriptCore의 모의 객체로 수행했고 테스트 코드에서 실제 Apple event를 보내지 않았다.
- release 앱을 0.1.1로 다시 패키징해 코드 서명을 확인했다. 실제 1040×700 관리 창에서 8개 세션과 각 TTY·PID를 확인하고, `ttys030` 검색이 기존 누락 세션 하나를 반환하는 동작을 확인했다. 화면은 `dist/qa/fixed-session-list.jpeg`에 저장했다.
- 사용자가 수정본에서 재연결한 뒤 앱의 로컬 상태 API로 `연결됨 · 7개 세션`과 7개 세션의 `terminalScreen` 채널을 확인했다. 전체 탐색은 8개이며 나머지 1개는 Claude가 만든 별도 백그라운드 PTY여서 `unknown`으로 구분됐다. 사용자 Terminal에 모델이 직접 입력하지 않았으며, 실제 권한 요청의 자동 승인 후 작업 재개 검증은 이번 연결 확인과 별개다.

아래는 0.1 최초 구현 시의 검증 기록이다.

## 기능 검증

| 검증 | 결과 | 범위 |
|---|---|---|
| Swift 핵심 검증 | 8/8 통과 | 권한 창 감지·오탐 fixture, 선택 옵션, PID+시작 시각 식별, 자식 프로세스 제외, 전경 그룹, 기본 비활성, 일시정지, 중복·세션 분리, 설정 보존·SQLite, 끊긴 연결 OFF·이동 가능 여부 |
| VS Code 화면 모듈 | 3/3 통과 | xterm 커서·지우기·대체 화면, 화면 해시·세대·만료·단일 사용, 미반영 출력이 있을 때 입력 차단 |
| 훅/소켓 통합 | 통과 | 실제 helper stdin → Unix socket → 승인 JSON, 조각난 메시지, 비활성·일시정지·중복 방지, 감사 기록·CLI status |
| PTY 화면 승인 통합 | 통과 | 임시 PTY 3개에 테스트 전용 실행 파일, 프로세스 발견·전경 검사, 정확한 대상, 동시 켜기, 같은 화면 한 번만 전송, 재개, 화면→훅 전환 시 예약 취소, 끊긴 세션 끄기 |
| 언어별 식별 | 통과 | 한국어·영어 환경의 실제 프로세스 ID가 같음. `ps` 실행에 C locale 고정 |
| 실제 세션 탐색 | 확인 | 현재 실행 중인 Claude/Codex의 PID·TTY·경로를 탐색. 자동 승인 기본 OFF 확인 |

테스트용 PTY는 직접 컴파일한 입력 대기 프로세스다. 가짜 VS Code 연결이 승인 메시지의 대상을 확인하고 응답한다. 실제 Claude/Codex 모델 호출, 실제 VS Code 터미널 입력, Terminal 자동화 권한을 검증한 결과로 해석하면 안 된다.

전체 Xcode가 없어 `swift test`의 XCTest 모듈을 사용할 수 없는 환경이므로, 동일한 핵심 검증을 Foundation 기반 `autoapprove-checks` 실행 파일로 제공한다.

## 실제 화면 확인

`computer-use`로 패키징한 네이티브 앱을 실행해 기본 크기 1040×700, 좁은 창 784×700, 최소 높이의 작은 창 약 784×612에서 라이트 화면을 확인했다.

- 실제 세션 목록, 연결 전 비활성 스위치, 세션 변경에 따른 상세 정보 전환.
- 검색 입력·지우기, 검색 결과 없음 화면.
- 자동 승인 필터 및 빈 결과.
- 연결 설정 시트, 각 연결 설명·설치 버튼, 스크롤 영역.
- 전체 일시정지 안내와 재개 후 상태 복원.

시각 검증은 빌드·기능 테스트와 별도로 실시했다. 다크 모드, VoiceOver 전체 흐름, 많은 승인 내역·긴 명령의 실제 렌더링은 아직 검증하지 않았다. 모바일·태블릿·웹 인터페이스는 이 앱의 대상이 아니다. 이번 실행의 화면 캡처는 `dist/qa/`에 저장했다.

읽기 전용 마무리 코드 검토에서 발견한 예약 취소·다중 켜기·끊긴 연결 제어·기존 훅 요청 안내·터미널 이동 가능 여부·메뉴 응답 수는 수정했고 재검토했다.

## 실제 CLI 연결에서 남은 검증

| 조합 | 탐색·구현 상태 | 실제 승인창의 입력 후 작업 재개 |
|---|---|---|
| Terminal + Claude | 실제 프로세스 탐색 확인, 화면 어댑터·훅 helper 구현 | 미검증 |
| Terminal + Codex | 실제 프로세스 탐색 확인, 화면 어댑터 구현 | 미검증 |
| VS Code + Claude | 프로세스 fixture·확장·훅 구현 | 미검증 |
| VS Code + Codex | 프로세스 fixture·확장 구현 | 미검증 |

이번 환경의 Computer Use 도구가 `com.apple.Terminal` 접근을 거부했다. 다른 경로로 실제 Terminal UI 접근을 우회하지 않았으며, Terminal 어댑터의 실환경 승인 성공을 주장하지 않는다. Claude 전역 훅과 사용자의 VS Code 확장을 자동 설치하지 않았고, 기존 사용자 세션에 승인 입력을 보내지 않았다.

VS Code의 안정 API는 확장이 `read()`를 호출한 이후의 출력만 제공한다. 이미 실행 중인 명령의 출력을 소급해서 연결할 수 없으므로 앱과 확장을 연결한 뒤 CLI를 다시 실행·이어하기 해야 한다. Claude 훅은 기존 세션의 설정 재적용 시점도 추가 확인이 필요하다.

Codex의 기존 공유 app-server 소켓은 이 환경에서 발견하지 못했다. 이번 버전은 app-server 연결을 제공하지 않는다. Terminal 화면의 재확인과 입력은 원자적이지 않으며, 일시정지는 이미 전달을 시작한 입력을 취소하지 않는다.

실제 설치 환경에서 권한 요청 종류·CLI 버전별 화면 호환성, 앱/확장 재시작, 잠자기·복귀, 10개 동시 세션과 지연 시간 측정은 후속 검증 항목이다.

## 재현 명령

```sh
swift build --disable-sandbox --cache-path .build/cache
.build/debug/autoapprove-checks
npm --prefix extensions/vscode test
node scripts/integration-check.mjs
node scripts/screen-integration-check.mjs
bash scripts/build.sh
```

최종 release에서도 `.build/release/autoapprove-checks`, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove`가 통과했다. 전체 빌드 스크립트로 앱·확장 패키지 생성 및 로컬 ad-hoc 서명을 확인했다. 최종 패키지의 `codesign --verify --deep --strict`와 내장 CLI 실행도 통과했고 앱을 다시 열어 기본 OFF 상태를 확인했다.

로컬 소켓·프로세스·PTY 접근을 차단하는 실행 샌드박스에서는 통합 테스트를 정상적인 로컬 개발 환경에서 실행해야 한다. 테스트는 임시 데이터와 테스트 프로세스를 종료·정리한다.
