# AutoApprove 검증 기록

최신 검증일: 2026-09-23. 환경: macOS 26.4 arm64, Swift 6.3 Command Line Tools, Node.js 22.22.2. ad-hoc 서명을 사용하며 Developer ID 서명·Apple 공증은 포함하지 않는다.

## 0.2.24 설치 스크립트 실행 안내 수정

- 다운로드한 `설치 및 실행.command`를 Finder에서 실행하면 스크립트 내부의 quarantine 제거 단계에 도달하기 전에 macOS가 차단할 수 있다. 기본 설치 경로를 터미널의 명시적인 `/bin/bash` 실행으로 바꾸고, DMG에는 실행 권한 없는 `install.sh`와 실제 버전의 복사 가능한 명령을 담은 `1. 설치 안내.txt`를 포함한다. 기존 0.2.23 파일의 실행 방법도 문서에 추가했다.
- `node scripts/install-check.mjs`의 설치 시나리오 **12/12** 통과. 다운로드 차단 속성이 붙은 비실행 스크립트의 명시적 bash 실행, 설치된 앱의 quarantine 제거, 원본 스크립트·앱의 속성 보존과 코드 서명을 검사한다. 기존 손상된 원본 거부·대상 검증·실행 중 앱 감지·설치 실패 롤백·권한 재시도·동시 실행 방지 검사도 유지했다. 첫 실행은 샌드박스의 프로세스 조회 제한으로 실패해 권한을 갖춘 격리 환경에서 다시 검사했다.
- 최종 DMG 사본에 다운로드 차단 속성을 붙여 읽기 전용으로 마운트했다. 안내에 적힌 `/bin/bash "/Volumes/AutoApprove 0.2.24/install.sh"` 명령으로 실제 배포 앱을 임시 설치 경로에 설치했다. 별도로 실제 앱·하위 실행 파일·설치 스크립트에 차단 속성을 붙인 경우도 통과했다. 설치된 앱의 quarantine 제거·엄격한 코드 서명·버전·원본과 실행 파일 해시 일치·임시 설치 파일 정리를 확인했다. 보고서: `.runtime/releases/0.2.24/installer-verification.json`.
- Finder의 실제 휴지통 경고 화면, 브라우저 다운로드, 실제 관리자 암호 입력과 설치 후 앱 실행은 이번 검사에 포함하지 않는다. 임시 경로에 `--no-open`으로 설치했으며 사용자 Terminal에 시험 입력을 보내지 않았다. Swift 코드는 변경하지 않고 이전에 검증한 0.2.23 릴리스 빌드를 **0.2.24 · 빌드 28**로 패키징했다. 실행 중인 사용자 앱은 교체하지 않았다.
- DMG의 코드 서명·이미지 체크섬·읽기 전용 마운트·Applications 링크·안내의 실제 명령·설치 스크립트 원문 및 비실행 권한 검증을 통과했다. SHA-256: `904907bd12598b9885d9680b1fd947e617db46d6057867d0f23788e3c4ff351b`. 이 DMG와 체크섬 파일을 [v0.2.24 릴리스](https://github.com/newdlops/autoapprove/releases/tag/v0.2.24)의 배포 파일로 사용한다.

## 0.2.23 반복 감시와 화면 갱신 최적화

- 실행 중인 0.2.22의 CPU 사용률은 순간 50~60%, 별도 20.4초 구간의 누적 CPU 시간 기준 **38.8%**였다. 읽기 전용 프로파일에서 14개 VS Code 창의 반복 등록이 프로세스 계보 재계산, 질문 중복 검사, 전체 스냅샷 발행과 SwiftUI 레이아웃을 반복하는 경로를 확인했다. 설치 스크립트 실행 시간의 측정값은 아니다.
- 변경 없는 등록은 세션 재매칭을 생략하되 터미널 크기는 계속 응답한다. 새 프로세스는 기존 탐색 루프에서 저장된 등록과 다시 연결한다. 일반 터미널의 화면 메시지, 짧은 상태 조회·Claude helper 연결 종료는 관련 없는 세션을 다시 발행하지 않는다. 화면 수신·승인 예약·상태 변경은 기존 경로로 처리한다.
- 동일한 세션 스냅샷은 UI·알림 구독자에게 재발행하지 않는다. 프로세스 계보 조회는 매칭 한 번에 인덱스 하나를 공유하고, 질문 중복은 스레드와 정규화된 제목별로 한 번씩 순회해 찾는다. 자동 승인 전의 프로세스·화면 재검증과 5초 질문 대기는 유지한다.
- 변경 전후 릴리스 코어를 사용하는 격리 성능 시험: 831개 프로세스 기록, 15개 세션, 8개 질문, 14개 브리지, 반복 메시지 **842건**. 스냅샷 발행 **1,262→29회(97.7% 감소)**, CPU 시간 **1.507→0.245초(83.8% 감소)**, 경과 시간 **8.994→8.066초**였다. UI를 렌더링하지 않는 고정 부하 측정이며 실제 앱 전체 체감 속도와 동일한 수치는 아니다. 모든 등록 응답과 변경된 터미널 이름의 즉시 반영도 확인했다.
- 릴리스 빌드 및 핵심 검사 **109/109**, 일반 훅/소켓 통합 검사와 실제 helper의 Claude 연속 승인·재시작·유실 응답 재전달 검사가 통과했다. Claude 통합 검사의 첫 시도는 샌드박스 소켓 제한으로 실패해 허용된 격리 환경에서 다시 검사했다. 새 회귀 검사는 변경 없는 발행 억제, 실제 변경의 즉시 발행, 한글 정규화·서로 다른 스레드·수동 편집 우선권·중복 제거를 확인한다.
- PTY 화면 통합 검사는 다른 검사와 함께 실행한 20초 제한에서 반복 재시도 단계가 시간 초과됐다. 같은 Mac의 프로세스 조회 한 번에 약 1초가 걸렸으며, 앱 코드는 바꾸지 않고 시험 전용 대기 한도를 30초로 늘려 단독 재실행한 전체 검사가 통과했다. 실제 프로세스 발견·제목 갱신·승인 대상·중복 방지·일시정지·유휴/작업 상태·훅 전환·실패 후 재시도·응답 유실 및 늦은 결과 격리를 검사했다.
- 최종 서명을 검증한 **0.2.23 · 빌드 27**로 로컬 실행 앱을 교체했다. 활성 9개·자동 승인 9개·VS Code 연결 14개 창을 확인했고, 보호 설정 18개 변경 0, 기존 감사 기록 손실 0, 동일 세션의 자동 승인 변경 0, 과거 질문의 신규 자동 응답 예약 0이다. 이전 앱과 DB는 `.runtime/releases/0.2.23/previous`에 보존했다.
- 교체 후 실제 앱의 별도 20.0초 측정에서 누적 CPU 시간은 1.72초, 평균 **8.6%**였다(교체 전 38.8%). 작업량·창 상태가 고정된 실험은 아니며 재실행 후 종료된 세션도 정리되므로, 변경 자체의 효과는 위 고정 부하 결과와 함께 판단한다. 측정 원본은 `.runtime/performance-{before,after}.json`, `.runtime/performance-live-{before,after}.json`에 보존했다.
- 0.2.23 DMG의 코드 서명·이미지 체크섬·읽기 전용 마운트·설치 스크립트와 안내 원문 검증을 통과했다. SHA-256: `492ca2c339f602614c69dc2b22ed7dd2d21d802cffed2b078c3eb1c77d176690`. 이 DMG와 체크섬 파일을 [v0.2.23 릴리스](https://github.com/newdlops/autoapprove/releases/tag/v0.2.23)의 배포 파일로 사용한다.
- 재현: `swift build --disable-sandbox --cache-path .build/cache -c release`, `.build/release/autoapprove-checks`, `node scripts/performance-check.mjs --assert-quiet`. 사용자 터미널에 시험 입력을 보내지 않았으며 화면 레이아웃은 변경하지 않았다.

## 0.2.22 질문 알림 대기 설정 · Codex 도구 허용

- Claude의 열린 승인 훅이 5초 자동 응답 예정인데도 수동 확인 후보로 포함돼 0.8초 만에 배너·소리·배지를 생성하던 경로를 수정했다. Claude와 Codex의 예약된 자동 응답은 수동 알림에서 제외하며 부모 설정을 상속한 자식에도 적용한다. OFF·일시정지·일반 선택 질문·터미널로 넘긴 요청은 알림 대상으로 유지한다.
- 사용자 지정에 따라 질문 배너·소리는 기본 10초 대기 후 한 번 발송한다. 연결 설정에서 1~3600초를 입력·적용하며 SQLite에 저장해 재실행 후 복원한다. 이미 기다린 시간은 설정 변경으로 초기화하지 않는다. 해결·열람·자동 응답 재개 시 예약을 취소하고 완료 알림은 기존 3초 확인 지연을 유지한다.
- 릴리스 빌드와 핵심 검사 **107/107** 통과. 30개 연속 Claude 자동 응답의 무알림, 부모·자식의 일반 권한·확인 질문, 수동 형제 질문과 배지 보존, OFF·일시정지·재개·터미널 넘기기, Codex 예약 질문의 무알림, 지연 기본값·경계·손상값 복구·저장 실패·재실행을 검사했다.
- 제공된 네 선택지의 `Cancel`과 옆 설명 때문에 일회 허용을 판별하지 못한 것을 수정했다. 로컬 Codex 바이너리에서도 같은 도구 승인 설명 문구를 읽기 전용으로 확인했다. `Allow preview?`·`Allow preview`·`Approve app tool call?`의 정확한 네 선택지, 설명 줄바꿈, 선택 위치 변경·일반 작업 선택지·키 안내 누락·후속 출력 거부를 검사했다. JavaScriptCore의 가상 Terminal 최종 입력 검사와 실제 격리 PTY/모의 VS Code 통합 검사에서 1번 한 번 전달·내용 변경 거부·중복 관찰 방지를 확인했다. 사용자 Terminal에 시험 입력을 보내지 않았다.
- `screen-integration-check.mjs --poll-timeout 20000` 및 실제 helper를 사용하는 `claude-hook-integration-check.mjs` 통과. 후자는 샌드박스의 로컬 소켓 생성 제한으로 첫 실행이 실패해 권한을 갖춘 격리 환경에서 다시 검사했다. 앱 재시작·응답 유실·정확한 재전달·감사 1건·터미널 넘기기가 유지됐다.
- `$ui-design-workflow`와 기존 네이티브 디자인 기준으로 **600×660 연결 설정**의 기본값, 0 입력 오류와 적용 비활성, 포커스, 5초 입력·Return 적용·현재 저장값 표시를 확인했다. 실제 macOS 알림을 쓰는 격리 `HighlightPreview.app`에서 새 질문 직후 Claude 알림 0건, 기본 **10.1초 후 1건**, 5초 설정 시 **5.1초 후 1건**, 1초 만에 해결한 질문의 알림 0건을 확인했다. 대기 중 60초에서 10초로 변경해도 최초 관찰부터 **10.0초 후 1건**이었으며 완료 알림은 별도 1건·반복 완료는 중복 0건이었다. 캡처는 `dist/qa/notifications-0.2.22-*.png`에 보존했다. 네이티브 앱이라 웹/모바일은 대상이 아니며 이번에 전체 VoiceOver·다크 모드·실제 사용자 터미널의 도구 실행 완료는 검사하지 않았다. 소리 설정은 포함하지만 실제 청취 여부를 검증한 것은 아니다.
- AutoApprove의 최근 알림에서 실제 전체 제목 `Allow Computer Use to use "HighlightPreview"?`, 네 선택지, `enter to submit | esc to cancel` 안내를 확인했다. 이 원문을 격리 화면 통합 검사에 넣고 최종 패키지의 helper로 재검사해 통과했다. Terminal 직접 접근은 사용하지 않았다.
- 실행 앱을 **0.2.22 · 빌드 26**으로 교체하고 배포 앱의 연결 설정에서 기본 10초를 확인했다. 교체 전후 활성 9개·자동 승인 9개, 보호 설정 14개 변경 0, 기존 감사 기록 손실 0, 같은 세션의 자동 승인 변경 0, 과거 질문의 신규 자동 응답 예약 0이다. Claude 관리 훅 8개와 나머지 설정도 보존됐다. 이전 앱·DB는 `.runtime/releases/0.2.22/previous`에 보존했다.
- 최종 DMG의 코드 서명·이미지 체크섬·읽기 전용 마운트·Applications 링크·설치 스크립트와 안내를 검증했다. `AutoApprove-0.2.22-macOS-arm64.dmg` SHA-256: `9b5e68a0be87160ae494f98fd3a3d9aaa233439aeda113c8537afa73c1594be0`. 이 DMG와 체크섬 파일을 [v0.2.22 릴리스](https://github.com/newdlops/autoapprove/releases/tag/v0.2.22)의 배포 파일로 사용한다.

## 0.2.21 앱에서 Claude 승인 · 연속 응답 복구

- 실제 `ttys032`에 연결된 백그라운드 기록의 30개 질문 중 23번은 17:12:50부터 17:13:54까지 약 64초 대기했다. 나머지는 대부분 1~5초 안에 결과가 기록됐다. 기존 helper의 소켓 대기는 2초, 관리 훅 제한은 4초였고 실패 시 재시도하지 않았다. 앱 재시작이나 지연 시 기존 질문을 터미널에 남길 수 있는 경로다. 실제 멈춤의 모든 원인을 이 하나로 단정하지 않는다.
- 원래 요청 UUID·내용 지문·실행 ID를 고정하고 공식 동기 훅을 유지한다. 자식 질문별 앱 승인, 요청별 5초 자동 응답, 부모 정책·일시정지·실행 관계 재확인을 구현했다. 응답과 감사 내역은 같은 SQLite 트랜잭션에 저장한다. helper 수신 확인 전에는 전달 대기로 표시하고 동일 요청 재시도에는 저장한 응답만 반환한다. [Claude 공식 훅 문서](https://code.claude.com/docs/en/hooks#pretooluse-decision-control)의 `allow`+`updatedInput.answers` 및 `PermissionRequest.decision`을 사용한다.
- Release 빌드 및 핵심 검사 **104/104** 통과. 추가 범위: 앱 허용 후 30개 연속 요청·개별 5초 지연, 응답 저장 전후 재시작, 유실 응답의 동일 내용 재전달·중복 방지, 동시 질문 분리, 일시정지와 직접 응답, 원래 터미널로 넘기기, 만료·PID 재사용·연결 해제, 열린 훅과 화면 입력의 중복 방지, 승인 직전 부모 종료, 응답·감사 동시 롤백과 재시도, 일반 실행 허용·비지원 질문 분리.
- 실제 release helper와 격리 Unix 소켓·비활성 PTY로 `claude-hook-integration-check.mjs` 통과: 5초 이상 수동 대기→앱 API 허용→다음 요청의 5초 자동 응답→대기 중 서버 재시작→프록시에서 승인 응답 1회 유실→정확한 응답 재전달과 감사 1건 유지→터미널 넘기기. `claude-parent-integration-check.mjs`, `integration-check.mjs`도 통과했다. PID 없는 새 helper는 원래 Claude 흐름을 유지하도록 검사한다.
- `$ui-design-workflow`와 Impeccable polish 기준으로 실제 네이티브 미리보기 **1040×700**, **784×612**를 확인했다. 허용·자동 승인 함께 켜기, 전달 중 중복 버튼 제거, 다음 질문 카운트다운·해결 후 제거, 긴 질문 줄바꿈, 저장 실패 오류와 복구 후 재시도를 검증했다. 캡처: `dist/qa/claude-app-approval-*.png`. 웹/모바일은 대상이 아니다. Tab 검색 포커스는 확인했으나 전체 키보드 탐색을 켠 승인 버튼 탐색, VoiceOver 전체 읽기, 다크 모드는 미검증이다.
- 기존 설치의 4초 제한이 새 5초 응답을 끊지 않도록 앱 시작 시 현재 번들의 helper와 정확히 일치하는 관리 훅만 시간 제한을 갱신한다. 기존 JSON을 백업하고 다른 helper·비활성화·제거된 훅·사용자 권한 설정은 보존한다. 임시 설정으로 이전 제한 업그레이드와 멱등성을 검증한다.
- 로컬 실행 앱을 **0.2.21 · 빌드 25**로 교체했다. 새 번들을 먼저 준비하고 서명을 검증한 후 기존 앱을 종료·교체·재실행했다. 실제 관리 훅 8개 중 `PreToolUse`·`PermissionRequest`는 660초, 나머지는 10초로 갱신됐으며 시간 제한을 제외한 기존 Claude 설정은 JSON 값 기준으로 일치했다.
- 교체 직전과 이후 모두 활성 세션 9개·자동 승인 8개다. 기존 감사 기록 손실 0, 과거 Codex 질문의 신규 예약 0, 보호 설정 14개 중 예상하지 않은 변경 0을 확인했다. 단, 기존 `/dev/ttys042`의 실행 상태는 OFF인데 저장값은 ON인 불일치가 있어 재실행 후 켜졌다. 교체 직전 OFF를 유지하도록 해당 저장값 1개를 명시적으로 보정했다. 이 기존 불일치의 일반적인 발생 경로를 이번 변경에서 수정한 것은 아니다. 보정 근거는 `.runtime/releases/0.2.21/preference-correction.json`, 이전 앱·DB는 `.runtime/releases/0.2.21/previous`에 보존했다.
- 최종 DMG의 코드 서명·이미지 체크섬·읽기 전용 마운트·Applications 링크·설치 스크립트 원문과 실행 권한·설치 안내를 검증했다. `AutoApprove-0.2.21-macOS-arm64.dmg` SHA-256: `e932408ef1375acb073860a4051d015bb05c20d919f242c549e6c55456430030`. 로컬 배포 파일이며 GitHub 릴리스는 발행하지 않았다.
- 사용자 Terminal에 시험 입력을 보내지 않았다. 수동 앱 승인→실제 Claude 도구 실행 완료는 사용자 세션에서 아직 검증하지 않았으며 임시 데이터 검증과 구분한다. 이미 반환된 구형 훅은 소급 처리하지 않고 일반 선택·다중 선택은 터미널에서 답한다.

## 0.2.20 ttys032 기존 질문 상태 복원

- 실제 Claude의 `agents --json --cwd /Users/lky/project/vcmfifth`에서 작업 `ca35b27f`, PID 45846이 `blocked / waiting / input needed`임을 확인했다. 부모 `ttys032`는 자동 승인 ON인데 AutoApprove 상태는 `unknown`이었다. 새 훅 없이 이미 열린 질문을 복구하지 못하는 것이 상태 누락의 원인이었다. 전역 AutoApprove 훅도 0개여서 배포 helper의 `install-claude`로 8개를 복원했다. 기존 설정은 `settings.json.autoapprove-23C6246B.backup`에 저장됐고, 관리 훅을 제외한 기존 설정은 보존됐다.
- PID·시작 시각이 일치하는 백그라운드 등록 정보의 현재 대기·작업·유휴 상태를 복원한다. 질문 전문은 작업 ID·대화 ID·대기 상태·갱신 시각이 모두 일치하는 선택적 구조화 메타데이터에서만 읽는다. 읽기 실패에는 대기 상태와 원래 터미널 안내를 표시한다. [Claude 공식 문서](https://code.claude.com/docs/en/agent-view#list-sessions-as-json)는 JSON 세션 조회를 지원하지만 작업 파일은 안정된 인터페이스가 아니라고 명시하므로, 작업 파일은 보조 표시 용도로만 사용하며 응답을 재생하지 않는다.
- 새로운 훅 처리 시각보다 오래된 상태는 무시해 이미 답한 질문이 다시 나타나는 것을 막는다. 훅 이후 늦게 갱신된 동일 대기 상태나 불완전한 질문 파일도 현재 훅의 질문 내용·식별자를 덮어쓰지 않는다. 반대로 검증된 다른 질문으로 바뀌었다면 누락된 새 훅을 기다리지 않고 복원한다. 상태 복원으로 자동 승인 설정을 바꾸거나 응답 기록을 만들지 않는다. 이미 반환된 훅 요청에는 소급 응답할 수 없다는 안내를 기존 상세 영역에 표시한다. 이번 수정은 기존 대기 질문에 대한 자동 입력 경로를 추가하지 않았다.
- `swift build --disable-sandbox --cache-path .build/cache -c release`, `.build/release/autoapprove-checks` **97/97**, 릴리스 helper의 `integration-check.mjs` 및 `claude-parent-integration-check.mjs` 통과. 실제 비활성 PTY·임시 Claude 등록/작업 파일·소켓으로 새 훅 없는 질문 복원, 다음 한글 질문의 `허용` 반환, 메인 설정 상속, 중복·일시정지·부모 종료를 검증했다. 잘못된 대화·작업 ID, 오래된 질문 파일, 손상 파일, 미래 시각, 상태 전환, 재시작과 읽음 유지도 검사했다.
- `$ui-design-workflow` 기준으로 기존 네이티브 화면을 재사용했다. 격리 `SessionPreview.app`의 **1040×700**, **784×612**에서 질문·세 선택지·현재 요청의 수동 처리 안내·배지 읽음·질문 해결 후 요청 제거를 기능/시각으로 각각 확인했다. `dist/qa/recovered-question-{standard,minimum,resolved}.png`에 저장했다. 웹·모바일은 대상이 아니며 이번 변경에 별도 디자인 체계나 브라우저 검증을 적용하지 않았다.
- 로컬 앱을 **0.2.20 · 빌드 24**으로 배포했다. 배포 직후 실제 `ttys032`가 `input`으로 표시되고 자식 `ttys037`의 ‘이 작업을 허용할까요?’와 세 선택지가 복원됐다. 보호 설정 14개 변경 0, 기존 감사 기록 손실 0, 기존 9개 세션 자동 승인 변경 0, 과거 Codex 질문의 신규 예약 0. 이전 앱·DB는 `.runtime/releases/0.2.20/previous`에 보존했다.
- 이어 17:03경 기존 질문에 `허용` 결과가 기록되고 현재 요청이 사라지는 것을 확인했다. AutoApprove의 자동 응답 기록이 아니므로 이를 자동 처리 성공으로 간주하지 않는다. 이후 같은 Claude가 보낸 작업 종류·진행 방식·검증 항목의 새 질문 3개가 정상 수신돼 메인의 `응답 필요`와 현재 요청에 함께 표시됐다(`dist/qa/claude-live-0.2.20.png`). 이는 예/아니오 확인이 아닌 선택형·다중 선택 질문이므로 직접 응답 대상으로 유지한다.
- Computer Use의 Terminal 접근 차단을 우회하지 않았고, 실제 Terminal 자동 입력은 검증하지 않았다. 등록/작업 메타데이터와 질문 기록을 읽기 전용으로 확인했으며 Claude 인증 키·내부 제어 소켓은 사용하지 않았다.
- DMG 코드 서명, 이미지 체크섬, 읽기 전용 마운트와 설치 스크립트를 패키징 과정에서 검증했다. `AutoApprove-0.2.20-macOS-arm64.dmg` SHA-256: `91627ce32165367f7a0e2de2cd56b31d129f22f6939913a6ca4e24f15084ea4e`. GitHub 릴리스는 발행하지 않았다.

## 0.2.19 Claude 메인·백그라운드 통합 · 한글 허용

- Claude 2.1.278의 로컬 등록 정보에서 PID 32068 (`ttys032`)의 `parkedJobId`와 PID 45846 (`ttys037`)의 `jobId`가 `ca35b27f`로 일치했다. 같은 프로젝트를 근거로 추측하지 않고 실제 PID·UTC 시작 시각을 대조했다. 등록 파일의 인증 키나 메시징 소켓에는 접근하지 않았다. [Claude 공식 agent view 문서](https://code.claude.com/docs/en/agent-view#switch-sessions-without-leaving-the-terminal)의 포그라운드 대화를 백그라운드로 옮기고 같은 터미널에서 다시 여는 동작과 대조했다.
- 백그라운드 실행은 메인 행과 스위치에 통합하고, 동시에 생긴 여러 질문·완료 알림을 보존한다. 읽음 기록은 실제 발생 세션별로 유지하면서 메인 열람 시 하나의 SQLite 트랜잭션으로 함께 저장한다. 감사 내역은 메인에 연결하되 원래 실행 ID·TTY·PID를 남긴다. 확인된 실행 관계는 재실행 후 복원하고 부모 종료·PID 재사용에는 상속 자동 승인을 멈춘다.
- 디버그 빌드 및 `.build/debug/autoapprove-checks` **94/94** 통과. UTC/현지 시각 대조, 모호한 부모·실제 조상, 메인 OFF와 자식의 기존 ON 충돌, 예·아니오/허용 및 일반 권한, 일시정지, 동시 동일 문구, 새 훅이 첫 탐색보다 먼저 도착하는 경우, 읽음·저장 실패·재실행·부모 종료·재사용·부모 화면 중복 응답 방지를 검사했다.
- 최종 릴리스 빌드와 `.build/release/autoapprove-checks` **94/94**, 릴리스 helper를 사용하는 `integration-check.mjs`, `claude-parent-integration-check.mjs`, `screen-integration-check.mjs --poll-timeout 20000` 모두 통과했다. 이전에 발행한 자식 알림이 통합 후에도 정확한 메인으로 이동하는 경로도 회귀 검사에 포함했다.
- `node scripts/claude-parent-integration-check.mjs .build/debug/autoapprove` 통과. 직접 컴파일한 비활성 PTY 프로세스 2개, 임시 Claude 등록 디렉터리, 격리 Unix 소켓과 VS Code 대역을 사용했다. 실제 `ps` 시각으로 부모를 연결해 한 행으로 표시하며, 메인 ON 후 helper stdin으로 보낸 한글 세 선택지에서 원문 `허용`을 반환했다. 실제 사용자 Claude에는 시험 질문이나 응답을 보내지 않았다.
- `SessionPreview.app`에서 네이티브 **1040×700**, **784×612**를 각각 확인했다. 자식 두 개가 메인 한 행에 포함되고, 메인 배지 1개가 상세 열람 후 0개가 된다. 메인 스위치 ON 후 자식 질문의 `허용` 전달과 메인 감사 내역 표시를 확인했다. 두 개의 긴 수동 질문은 동시에 보존되어 세로 스크롤로 읽으며, 자식 TTY 검색은 메인 한 행을 반환한다. 화면 캡처는 `dist/qa/parent-*.png`에 저장했다.
- `$ui-design-workflow`와 Impeccable polish 기준으로 기존 네이티브 목록·시스템 글꼴·상태 색상·포커스·긴 내용·스크롤을 확인했다. 새 질문 영역은 최근 알림보다 위에 둔다. 웹/모바일은 대상이 아니며 VoiceOver 전체 동작, 다크 모드 및 실제 Terminal 창의 자동 응답 후 작업 재개는 이 검사에 포함되지 않는다. Terminal 기본 탭·Dock 배지 복제는 별도 미구현 요구사항으로 유지한다.
- 로컬 배포 앱을 **0.2.19 · 빌드 22**로 교체하고 실제 도움말에서 버전을 확인했다. 실제 `ttys037`이 ‘딜 추천 품질 검토’의 `ttys032` 내부로 들어가 활성 목록은 10개에서 9개로 표시된다. 메인 스위치 ON과 자식 상속 ON, 이전 자식의 질문 알림·승인 내역이 메인 상세에 모이는 것을 확인했다. 교체 직전 보호 설정 14개 변경 0, 기존 승인 이벤트 손실 0, 기존 Codex 질문의 신규 자동 응답 예약 0이다. 이전 앱과 DB는 `.runtime/releases/0.2.19/previous`에 보존했다.
- 배포 직전 사용자 환경의 Claude 훅이 **16:16:20 KST에 해제된 상태**임을 발견했다. 실행 앱은 ‘훅 연결 해제됨’이었고 관리 훅 8개가 없어졌으며, `ttys035`와 자식 `ttys037`의 독립 설정도 OFF였다. 이번 검사의 훅 설치·제거는 모두 명시적인 임시 설정 파일을 사용했다. 해제 주체·의도는 확정하지 않았고 사용자에게 재연결 여부를 확인했다. 최신 설정은 보존했으며, 최종 확인 시 Claude 훅은 ‘설치 필요’이고 사용자 Claude의 새 질문 자동 응답은 실환경 검증 전이다.
- 최종 DMG의 코드 서명·이미지 체크섬·읽기 전용 마운트·설치 스크립트 원문과 실행 권한을 검증했다. `AutoApprove-0.2.19-macOS-arm64.dmg` SHA-256: `00516179e2f09751aab4f3e15f4117f0af8c9575292721b62cb388c07e344f19`. 로컬 배포 파일이며 GitHub 릴리스를 발행하지 않았다.

## Claude 한글 예·아니오 미응답 · ttys032 연결 복구

- 사용자가 지정한 ‘딜 추천 품질 검토’는 `/dev/ttys032`, PID `32068`, `vcmfifth`의 Claude 세션이다. 실제 AutoApprove 상태는 자동 승인 켜짐·전체 일시정지 꺼짐·Terminal 화면 연결이었으며, Claude 질문 훅은 설치되지 않았다. 이전에 조사한 자동 승인 꺼짐 상태의 `ttys037`과 다른 세션이다.
- 최근 대화 기록에서 `예/아니오로 답해 주세요. 진행할까요?`와 `예` / `아니오` 선택지를 가진 `AskUserQuestion`이 2026-09-22 06:34:16 UTC에 기록된 뒤 응답 없이 남아 있었다. 화면 연결의 권한 확인 판별기와 별도로 처리하는 질문 훅 경로에 이 요청이 전달되지 않았다.
- 배포된 **0.2.18** helper와 별도 임시 데이터·Unix 소켓을 사용해 실제 질문 JSON의 CLI 훅 왕복을 확인했다. 자동 승인 켜짐에서 원래 질문·선택지를 보존해 `updatedInput.answers`에 `예`를 반환하고, NFC/NFD 한글·자동 승인 꺼짐·일시정지·같은 요청 중복·저장된 응답 2건을 검사했다. 사용자 Claude 세션으로 시험 질문이나 답변을 보내지 않았다. [Claude 공식 훅 문서](https://code.claude.com/docs/en/hooks#pretooluse-decision-control)의 `allow`와 `updatedInput` 응답 형식도 대조했다.
- `autoapprove install-claude`로 사용자 설정의 AutoApprove 훅 8개를 복구했다. 기존 설정은 `~/.claude/settings.json.autoapprove-EC1878FE.backup`에 보존했다. 새 관리 훅만 제외한 설정이 백업 원문과 JSON 값 기준으로 일치하고, 각 이벤트에 올바른 배포 helper·데이터 경로를 사용하는 관리 훅이 하나씩 있는지 확인했다. `ttys032`의 자동 승인 켜짐도 유지됐다.
- 설치 전에 이미 열린 질문에는 새 훅이 소급 실행되지 않는다. 사용자가 해당 질문을 답하거나 닫고 새 질문을 요청한 뒤 실제 응답을 확인하도록 안내했다. 복구 직후에는 새 질문 이벤트가 없어 실제 Claude에서의 후속 성공은 아직 확인하지 않았다. 앱 코드·버전·배포 파일은 변경하지 않았다.

## Terminal 기본 배지 · 요구사항 확인과 연결 조사

- 사용자가 배지 원본을 Terminal 앱의 탭·Dock 알림으로 확정했다. AutoApprove 안에서만 표시 이름을 바꾸고 해당 세션 상세 열람 시 앱 안의 배지를 읽음 처리한다는 앞선 선택도 설계에 기록했다. 0.2.18의 질문·작업 완료 기반 배지로 이 연동 요구사항이 완료됐다고 판단하지 않는다.
- [Apple 문서](https://support.apple.com/en-lamr/guide/terminal/trmladvn/mac)에서 Dock 숫자가 벨 문자를 받은 백그라운드 세션의 수임을 확인했다. 설치된 Terminal의 `Contents/Resources/Terminal.sdef`를 정적으로 읽었으며 탭의 TTY·내용·제목·선택 속성은 있지만 벨/배지 상태 속성은 없었다. 정적 리소스와 실행 파일 문자열도 조사했지만 지원되는 외부 조회 경로는 확인하지 못했다.
- 접근성 경로가 원본 벨 상태와 TTY 식별을 함께 제공하는지는 미확인이다. Computer Use의 기존 차단 사유는 `Computer Use is not allowed to use the app 'com.apple.Terminal' for safety reasons.`이며, 다른 UI 자동화 경로로 우회하지 않았다. 실제 Terminal 배지 발생·세션 대응·열람 후 새 알림을 검증하지 않았다.
- 이번 후속 작업은 설계와 검증 기록만 갱신했다. 앱 코드·실행 앱·배포 파일은 변경하지 않았고 빌드나 기능 검사는 다시 실행하지 않았다. Terminal 기본 배지 연동은 미구현 상태다.

## 0.2.18 세션 표시 · 알림 배지 · Claude 가상 터미널

- 디버그·릴리스 빌드와 `.build/debug/autoapprove-checks` **85/85**가 통과했다. 표시 설정 정규화·검색·초기화·저장 실패·재실행·실행 ID 분리, 알림 지연·읽음·같은 문구의 새 요청·초기 탐색 중 읽음 보존·최근 50건 한도를 검사했다.
- 최종 배포 대조에서 기존 버전의 Claude 훅 해제가 자동 승인 꺼짐을 메모리에만 반영해 재실행 후 옛 켜짐 설정을 복원하는 문제를 발견했다. 해당 세션을 직전 꺼짐 상태로 복원했으며 그 사이 새 승인 이벤트는 없었다. 훅 해제 전에 꺼짐 설정을 저장하도록 수정하고 저장 실패·재시작 회귀 검사를 추가했다. 최종 릴리스 빌드와 `.build/release/autoapprove-checks` **86/86**가 통과했다.
- 사용자 제공 `계속 진행할까요? / 예 / 아니오` JSON 그대로 자동 승인 꺼짐과 켜짐을 비교했다. 켠 뒤 새 질문에 `answers: {"계속 진행할까요?": "예"}`가 전달된다. 실제 과거 기록 두 요청도 정상 인식됐으며 당시 해당 실행 ID의 자동 승인이 꺼져 있었다. 미응답 사유를 상세에 표시한다.
- `ttys037`의 실제 프로세스·부모·표준 입출력과 Claude 실행기 기록을 읽기 전용으로 확인했다. 부모가 `ClaudeCode.app` 백그라운드 실행기이고 내부 PTY인 구조를 재현해 `Claude 백그라운드`로 분류한다. 다른 Terminal/VS Code 탭으로 대신 연결하지 않으며 원래 Claude 화면으로 이동하는 기능은 추가하지 않았다.
- `node scripts/integration-check.mjs` 및 `node scripts/screen-integration-check.mjs .build/debug/autoapprove --poll-timeout 20000`가 통과했다. 격리 소켓의 한글 질문·승인·완료 훅 왕복과 실제 격리 PTY의 발견·화면 승인·훅 전환·재시도·늦은 결과·연결 해제를 확인했다. 사용자 터미널로 시험 답변을 보내지 않았다.
- 실제 화면 코드와 격리 DB를 쓰는 `SessionPreview.app`에서 520pt 편집 시트의 이름·여러 줄 한글 메모·파랑 저장, 81자 이름 제한과 저장 비활성화, 초기화 후 취소, 한글 메모 검색·결과 없음, 1040×700와 784×612 표시를 확인했다. 둘째 세션 열람 시 둘째 배지만 사라지고 셋째는 남으며 질문 상태가 유지된다. 재실행 후 이름·메모와 읽음/안 읽음 상태도 보존됐다.
- `$ui-design-workflow`와 Impeccable의 독립 마무리 검토를 적용했다. 선택 행에서 파란 태그가 배경과 섞이는 지적을 반영해 중립 배경을 추가하고 최종 렌더를 확인했다. 접근성 이름·선택 상태를 검사했으며 VoiceOver나 구형 macOS 기기 검증은 수행하지 않았다. 화면 기록은 `dist/qa/session-*.png`에 있다.
- 추가 네이티브 확인: 저장 실패 시 시트에 오류가 표시되고 이름·메모·색상 초안이 보존된다. 긴 이름과 25줄 메모는 최소 창에서 목록 한 줄 요약과 상세 스크롤로 표시된다. 실행 앱의 도움말에서 버전 **0.2.18 · 빌드 21**, 새 이름·메모·색상 및 배지 안내를 확인했다.
- 배지는 앱이 감지한 질문·작업 완료 기준이다. Terminal 탭·Dock 배지 자체는 공식 연결에 노출되지 않으며 Computer Use도 Terminal 접근을 차단해 복제하거나 화면 검증하지 않았다. 새 macOS 알림 배너의 실전 전달·클릭은 이번 변경에서 대역 UI와 별도로 재검증하지 않았다.
- 최종 로컬 앱 **0.2.18 (21)**을 재실행했다. 활성 10개·자동 승인 8개, 직전 보호 설정 14개 변경 0, 기존 승인 이벤트 손실 0, 기존 질문의 새 자동 응답 예약 0을 확인했다. 데이터와 교체 전 앱은 `.runtime/releases/0.2.18/previous`에 백업했다. 옛 꺼짐 설정 복원 전후 대조도 별도 보존했다.
- 최종 DMG는 코드 서명·이미지 체크섬·읽기 전용 마운트·설치 스크립트/안내 원문과 실행 권한 검사를 통과했다. `AutoApprove-0.2.18-macOS-arm64.dmg` SHA-256: `b589b70b51f83c9caeea75156eff154096939f221193e63893b84b1ae18d643f`. 로컬 배포 파일이며 GitHub 릴리스를 발행하지 않았다.

## 0.2.17 설치 및 실행 스크립트

- DMG에 `설치 및 실행.command`를 포함한다. 번들 식별자·실행 파일·코드 서명을 확인하고 같은 설치 폴더의 임시 위치에 복사한 뒤, 복사본의 `com.apple.quarantine`만 재귀적으로 제거하고 앱을 교체·실행한다. 설치 위치 또는 기존 앱이 쓰기 보호되어 있거나 속성 제거에 권한이 필요하면 `sudo` 인증을 요청한다. 암호는 `sudo`가 직접 받으며 스크립트는 읽거나 저장하지 않는다.
- `node scripts/install-check.mjs`의 **11개 시나리오**를 통과했다. 별도 ad-hoc 서명한 테스트 앱에 실제 quarantine 속성을 넣고, 앱 및 하위 파일의 차단 표시 제거·다른 속성 및 이웃 앱 보존·반복 설치·손상 파일 거부·다른 앱/심볼릭 링크 교체 거부·실행 중 교체 방지·최종 이동 실패 시 복원·인증 취소·권한 오류 후 재시도·기존 앱 쓰기 보호·동시 설치 잠금·일반 사용자로 앱 열기 요청을 확인했다. 하나의 시나리오가 여러 조건을 검사한다.
- 공백·한글·작은따옴표·달러 기호가 포함된 경로를 사용했다. `/var`와 `/private/var`처럼 같은 파일의 경로 표기가 다른 경우에도 실행 중인 설치 대상의 파일 동일성을 비교해 교체를 막는다. 최초 검사에서 샌드박스가 `ps`를 차단해 허용된 실행 환경에서 다시 검사했다.
- 실제 비밀번호 입력·관리자 권한 상승·Finder의 `.command` 더블클릭·Gatekeeper 경고 UI는 실행하지 않았다. `sudo` 인증 성공/실패와 앱 열기 요청은 격리된 검사 복사본에만 대역을 주입했다. 최종 배포 스크립트는 절대 경로의 시스템 명령을 사용하며 대역 설정을 받지 않는다. Terminal 안내와 `--help` 출력은 명령 실행으로 확인했으며 네이티브 앱 UI는 이번 설치 변경에서 수정하지 않았다.
- DMG를 여는 것만으로 자동 실행되지는 않는다. 스크립트 자체가 차단될 때 터미널의 `/bin/bash` 뒤로 스크립트를 드래그해 실행하는 절차를 설치 안내와 README에 포함했다. 시스템 전체의 Gatekeeper 설정과 다른 앱의 속성은 변경하지 않는다.
- 최종 DMG 복사본에 다운로드 quarantine 속성을 주입하고 읽기 전용으로 마운트한 뒤, 포함된 스크립트로 실제 **0.2.17 (20)** 앱을 격리 폴더에 설치·재설치했다. 설치 결과의 quarantine 부재, 엄격한 코드 서명, 원본과 GUI/helper 바이너리 해시 일치, 임시 폴더·잠금 정리를 확인하고 이미지를 추출했다. 이 CLI 마운트에서는 원본 앱에 quarantine 속성이 노출되지 않았으므로, 앱 및 하위 파일의 실제 속성 제거는 위 테스트 앱 검사로 확인했다. 브라우저 다운로드나 Gatekeeper UI 통과를 재현한 검사는 아니다.
- `node scripts/package-dmg.mjs`의 이미지 검증·읽기 전용 마운트·앱 서명·Applications 링크·설치 안내 및 스크립트 원문 일치·스크립트 실행 권한 검사가 통과했다. 최종 `AutoApprove-0.2.17-macOS-arm64.dmg` SHA-256은 `313c571e9ddcefa946241ccc44a257ca58b965d3ccd0889636b9f6eb6c1aa2f6`이다. 기존 실행 앱과 사용자 데이터는 이번 설치 검사에서 교체하거나 실행하지 않았다.

## 0.2.17 앱 정보 · 도움말 · 메뉴 정리

- 실제 번들의 버전·빌드 번호를 도움말과 macOS 기본 정보 패널에서 확인한다. 버전 정보 복사 후 격리 앱의 검색란에 붙여넣어 `AutoApprove / 버전 0.2.17 · 빌드 20 / macOS 버전 26.4(빌드 25E246)`을 확인했다. 복사 완료 표시와 기본 붙여넣기·전체 선택·삭제도 확인했다.
- 도움말의 시작하기·자동 응답·문제 해결, 마지막 문단까지 스크롤, 주제 전환 시 스크롤 초기화를 실제로 조작했다. 최종 [기본 도움말 620×660](dist/qa/help-0.2.17-default.jpg), [최소 도움말 520×572](dist/qa/help-0.2.17-minimum.jpg), [최소 관리 창 784×612](dist/qa/help-0.2.17-main-minimum.jpg), [기본 앱 정보 패널](dist/qa/about-0.2.17.jpg)을 확인했다. 최소 도움말은 콘텐츠 높이 540pt에 시스템 제목 막대를 포함한 크기다. 기존 앱 아이콘·시스템 글꼴·구분선·여백을 사용하며, 제목의 접근성 머리말과 컨트롤의 이름·설명도 확인했다.
- 실제 메뉴에서 빈 포맷·보기·문서·서비스 메뉴, 편집의 자동 완성 하위 메뉴, 복잡한 창 타일 배치 항목을 정리했다. 실행 앱의 상위 메뉴는 `AutoApprove / 편집 / 윈도우 / 도움말`이다. 편집의 기본 입력 동작·받아쓰기·이모지, 창 닫기·최소화·확대/축소와 관리 창·승인 내역 진입점을 유지한다. macOS가 추가하는 디스플레이 이동 항목은 표시될 수 있다.
- 툴바·도움말 메뉴로 도움말 열기, 앱 정보, 연결 설정 열기/완료, 관리 창 메뉴, `⌘O`, `⌘,`, `⇧⌘H`, `⌘W`, `⌘M`과 최소화 후 복원을 확인했다. `⇧⌘/`는 이 macOS의 도움말 검색에 가로채여 직접 열기를 `⌘/`로 정하고 최종 앱에서 검증했다. 실제 실행 앱에서 창이 없는 상태로 닫힌 뒤에도 `⌘/`로 도움말을 다시 열었다.
- 릴리스 빌드와 엄격한 코드 서명 검증을 통과했다. 승인 엔진·확장 코드는 변경하지 않아 기존 0.2.16 핵심·통합 검사를 반복하지 않고 이번에 바뀐 네이티브 UI 경로를 검증했다. 최종 구현의 네이티브 시각 검토에 `ui-design-workflow`와 Impeccable을 적용했다. 브라우저용 화면은 없으며 외부 링크는 접근성에 노출된 URL을 대조했다. 브라우저 실행·다크 모드·VoiceOver 전체 탐색·클립보드 쓰기 실패 주입은 이번 검사에 포함하지 않았다.
- 기존 앱과 SQLite를 백업하고 실행 앱을 **0.2.17 (20)**로 교체했다. 활성 **10개**, 자동 승인 **9개**, 보존 대상 설정 **13개**의 변경과 기존 승인 내역 **206건**의 유실은 모두 **0건**이다. Terminal 연결 9개와 [실행 앱의 도움말](dist/qa/help-0.2.17-production.jpg)을 확인했다.
- DMG의 서명·체크섬·읽기 전용 마운트·Applications 링크·설치 안내를 검사하고 정상 추출했다. 이후 설치 스크립트를 추가한 최종 DMG 검증은 위 항목에 기록한다. macOS 14 이상 Apple Silicon용이며, 내장 VS Code 확장은 0.2.3이다.

## 0.2.16 배포

- 릴리스 빌드와 핵심 검사 **76/76**, VS Code 확장 검사 **5/5**, 릴리스 helper의 훅/소켓 및 PTY 화면 통합 검사가 통과했다. 화면 검사는 앞서 확인한 이 Mac의 프로세스 조회 지연을 반영해 `--poll-timeout 30000`으로 실행했다.
- 최초 릴리스 검사에서 개별 입력/취소 테스트가 고정 5.3초 뒤 전송을 단정해 75/76이었다. 해당 검사를 기존 제한 시간 안에서 미편집 질문의 접수 완료를 기다리도록 고쳤고 전체 76/76을 다시 확인했다. 실제 앱의 5초 지연과 조기 전송 금지 검사는 유지했다.
- VS Code 확장 0.2.3을 고정된 의존성으로 다시 패키징했다. 첫 npm 설치는 샌드박스 DNS 제한으로 실패해 허용된 네트워크에서 재실행했다. 의존성 검사 결과 취약점 0건이었다.
- 이전 앱과 SQLite 저장소를 백업한 뒤 실행 중인 `dist/AutoApprove.app`을 정상 종료하고 **0.2.16 (19)**로 교체·재실행했다. 활성 **10개**, 자동 승인 **9개**, 동일 세션의 자동 승인 변경 **0개**다. 기존 자동 승인·순서·일시정지·Terminal 설정 **13개**와 승인 내역 **199건**의 보존을 대조했다. Terminal 연결 9개와 앱 화면의 새 Yes·Allow 안내를 확인했다.
- DMG의 엄격한 코드 서명, 체크섬, 읽기 전용 마운트, Applications 링크, 설치 안내를 확인하고 정상 추출했다. `AutoApprove-0.2.16-macOS-arm64.dmg`의 SHA-256은 `5cb9c8ce22192fba1b6a299e2689af2a6e22536004848cebe1eef5ef66b74a13`이다. macOS 14 이상 Apple Silicon용이다.
- 변경된 네이티브 UI와 Codex 격리 메시지 큐의 검증 범위는 아래 ‘응답 자동화 보완 및 Allow 인식’에 기록했다. 사용자 대화에 시험 답변을 보내지 않았으며, 구형 macOS 실기·전체 VoiceOver·다크 모드는 추가 검증하지 않았다.

## 응답 자동화 보완 및 Allow 인식

- 선택지 조작·입력란 포커스·직접 입력·질문별 취소는 즉시 예약을 멈춘다. 반복 조회·전체 재개·앱 재실행에서도 직접 답변/개별 취소 상태를 유지하며, 저장 실패 시에도 현재 예약은 중지된다. 다른 세션이나 질문의 예약은 유지한다.
- 루트 대화의 첫 조회에 있던 질문, 같은 문구의 중복 질문, 인용 없는 후속 메시지가 있는 질문은 직접 확인하도록 표시한다. 연결 복구로 과거 질문이 새 질문으로 바뀌지 않고, 전송 준비 단계에서도 최신 기록의 중복·후속 메시지를 확인한다.
- 전송 준비 중 취소는 실제 실패와 구분한다. 준비가 끝나기 전에 전체 일시정지/자동 승인 해제를 했다가 재개하면 새로 5초를 기다린다. 이미 접수를 시도한 결과 미확인 응답은 자동 재전송하지 않는다.
- `Allow`·`Allow once`·대소문자·추천 표시·단축키, `Deny`·`Don't allow`·곡선 따옴표를 검사했다. 반복 허용 선택지가 함께 있으면 유일한 이번 요청 허용을 선택한다. 부정 표현·비슷한 단어·여러 일회 허용 선택지·영구 허용만 있는 선택지는 자동 선택하지 않는다. Codex 자동 응답에서 실제 5초 지연과 원래 Allow 라벨·감사 기록을 확인했다.
- 디버그 빌드와 핵심 검사 **76/76** 통과. 새 검사는 실시간 지연, 입력/취소의 지속성, 과거 기록, 준비 중 취소/재개, 최신 기록 확인, Allow 판별·전송을 포함한다.
- 최종 코어의 훅/소켓 통합 검사와 설치된 Codex CLI의 격리 메시지 큐 검사도 통과했다. 정확한 대화, 여러 줄 답변, 셸 특수 문자의 원문 보존을 확인했다.
- PTY 화면 통합 검사도 통과했다. 이 Mac에서는 기본 8초 제한으로 초기 프로세스 발견과 주기적인 Git 갱신에서 시간 초과가 발생했다. 검사 전용 `--poll-timeout 30000` 옵션으로 재실행해 전체 시나리오를 완료했다. 기본 제한은 8초이며 앱의 지연·재시도 설정은 이 옵션과 무관하다.
- 실제 production `SessionWindow`를 사용하는 격리 네이티브 앱의 **1040×700**, **784×612**에서 [카운트다운](dist/qa/automatic-question-countdown.png), [긴 질문/선택지](dist/qa/automatic-question-minimum.png), [개별 취소](dist/qa/automatic-question-cancelled-minimum.png), [입력 포커스](dist/qa/automatic-question-draft.png), [접수 완료](dist/qa/automatic-question-queued.png), [중복·후속 메시지](dist/qa/automatic-question-history.png), [전송 오류](dist/qa/automatic-question-send-error.png), [빈 목록](dist/qa/automatic-question-empty.png)을 확인했다. No 선택 후 전체 재개, 취소 버튼, 영어 직접 입력, Tab으로 검색란 이동, 일시정지 중 직접 답변 접수도 조작했다. 연결 오류 중에는 사용할 수 없는 입력 안내를 숨기도록 보완했다.
- 최종 네이티브 빌드에서 [Allow 카운트다운](dist/qa/automatic-allow-countdown.png) → [원래 Allow 라벨로 접수](dist/qa/automatic-allow-queued.png)를 확인했다. [최소 창의 긴 카운트다운](dist/qa/automatic-question-long-countdown.png)은 상태 줄을 감싸고 취소 버튼을 아래에 배치한다. [연결 오류 안내](dist/qa/automatic-question-connection-error.png)의 최종 문구·조작 경로도 다시 확인했다.
- 재현: `swift build --disable-sandbox --cache-path .build/cache` 후 `node scripts/package-automatic-question-preview.mjs`. 검증 앱의 ‘검증’ 메뉴에서 시나리오와 창 크기를 바꿀 수 있다. 메시지 전송·알림·터미널 이동은 대역을 사용하며 상태는 `dist/qa/automatic-question-state.json`에 기록한다. 사용자 대화에 시험 답변을 보내지 않았으며 설치된 앱 교체는 수행하지 않았다. 다크 모드·VoiceOver 전체 탐색·실제 hover 툴팁 팝업은 이번 시각 검증에 포함하지 않았다.

## 응답 대기 5초 후 Yes 자동 응답 · 최초 구현

- Codex 예·아니오 질문마다 실제 5초 이상 기다린 뒤 원래 Yes 선택지를 보내는 것을 전송 대역으로 확인했다. 반복 조회가 시간을 초기화하지 않고, 뒤늦게 추가된 질문은 별도로 5초를 기다리며, 다시 묻지 않기보다 이번 요청의 Yes를 선택한다.
- 자동 승인 끄기·일시정지·연결 해제·질문 정리/해결·조회 오류/일시적 읽기 보류·세션 종료·엔진 종료·수동 No 전송에서 예약을 취소한다. 재개·질문 변경은 새로 5초를 기다린다. 전송 준비 도중 설정이나 질문이 바뀌면 보내지 않고, 오래된 결과가 새 질문을 덮어쓰지 않는다.
- 전송 전 감사 기록과 예약 저장, 다른 세션의 잘못된 응답 요청 격리, 전송 전 검증 실패·길이 제한·저장 실패·접수 결과 미확인, 반복 조회와 앱 재실행 후 중복 방지를 검사했다.
- 디버그 빌드와 핵심 검사 **71/71**, 훅/소켓 통합 검사, 설치된 Codex CLI의 격리 대화 메시지 큐 검사, PTY 화면 통합 검사가 통과했다. 화면 통합 검사는 다른 검사와 동시 실행할 때 재시도 대기에서 한 번 시간 초과가 났고, 단독 재실행은 통과했다.
- 사용자 세션에 시험 답변을 보내지 않았다. UI 코드는 변경하지 않았으며 실제 앱 창의 시각 검증과 배포 앱 교체는 수행하지 않았다.

## 0.2.15 빠른 화면 갱신·승인 복구

- 사용자는 서로 다른 명령 사이의 간격이 짧을 때 멈춘다고 보고했다. 기존 1,000회 검사는 승인 메시지를 받으면 바로 다음 질문을 보내지만, 즉시 성공 응답하는 모의 연결만 사용했다. 추가 검사에서 다음 질문까지 **0·1·5·10·25·50ms**, 이전 응답 **250/1,500ms 지연 및 이전 요청 실패**를 각 10회씩 주입했다. 이 60개 요청의 짧은 간격만으로는 기존 0.2.14에서 멈춤이 재현되지 않았다.
- 별도 0.2.14 재현: 입력을 이미 예약한 뒤 확인 안내가 잠깐 사라졌다 돌아오는 화면을 보내면 **중복 승인 입력**이 발생했다. 또한 네 번 연속 입력 미전달을 응답하면 화면이 안정된 뒤에도 `pendingInTerminal: true`, `화면 확인이 반복해서 실패했습니다`에 고정됐다. 이는 격리된 테스트 재현이며 사용자의 다른 Mac에서 같은 원인이 발생했다고 직접 확인한 것은 아니다.
- 수정: 불완전한 재그리기는 기존 승인 예약을 보존하되 그 화면에서 입력하지 않는다. 확인된 작업·입력 대기 전환, 새 Codex 명령·연결 세대는 구분한다. 같은 요청의 줄바꿈·단축키 변경은 진행 중인 프로세스 확인에 합쳐 처리하고, 입력 직전에는 **최신 완전한 화면 원문**을 어댑터로 전달해 다시 검증한다.
- 입력이 전혀 전달되지 않았다고 확인된 경우에만 **0.25→0.5→1→2→최대 4초** 간격과 다음 화면 수신을 요구하며 재시도한다. 영구적인 횟수 소진을 제거했다. 실제 성공·결과 미확인·8초 전달 확인 시간 초과는 재전송하지 않으며, 불완전한 재그리기 후에도 확인 필요 사유를 보존한다. VS Code 응답의 누락·null·숫자·문자열을 미전달로 취급하지 않고 실제 JSON boolean 응답만 처리한다.
- 릴리스 빌드 및 핵심 검사 **67/67**, 훅/소켓, Codex 전용 연속 승인, 기존 PTY 화면 통합 검사가 통과했다. 새 `--race` 검사는 위 60개 요청, 늦은 이전 응답, 불완전한 재그리기, 실제 작업 전환 후 같은 명령의 새 승인, 불완전 화면에서 자동 승인 켜기·일시정지 해제, 네 번 미전달 후 **다섯 번째 한 번만 전달**, 재시도 간격 검사를 통과했다. 최종 앱 번들에서도 미전달 복구 및 잘못된 응답·시간 초과·재그리기 후 경고 보존 검사를 통과했다.
- 앱 **0.2.15 (18)**을 실행했다. 활성 **10개**, 자동 승인 **9개**, 기존 세션의 자동 승인 변경 **0개**이며 순서·일시정지·Terminal 연결 설정을 보존했다. UI 코드는 바뀌지 않아 새 레이아웃 검사는 수행하지 않았다. Computer Use로 업데이트된 앱의 실행을 확인했다.
- DMG의 코드 서명·체크섬·읽기 전용 마운트·Applications 링크·설치 안내를 확인하고 정상 추출했다. SHA-256: `334f6722ae58ef2a4e5c05e1b63bde953f9838fd92815a4244d7fa39e1e11c4a`. macOS 14 이상 Apple Silicon용, 내장 VS Code 확장 0.2.3이다. 격리 PTY와 모의 VS Code 연결로 검증했으며 구형 macOS 실제 Terminal의 입력·작업 재개는 미검증이다.

## 0.2.14 연속 승인 1,000회 부하 검사

- 배포 앱의 내장 helper로 한 세션에서 서로 다른 Codex 승인 요청을 **1,000회 연속 처리**했다. 같은 프로세스·화면 세대를 유지하고 요청 사이에 작업 화면을 넣지 않았다. 모든 응답의 대상·원문·선택값 `1`·고유 입력 ID를 검사했으며 첫 1,000건에서 누락·중복·잘못된 응답은 **0건**이었다.
- 첫 1,000건 소요 시간 **488초(8분 8초)**, 승인 메시지 수신 지연 **p95 851.5ms / 최대 1,481.3ms**. 10건마다 같은 화면과 줄바꿈·이전 출력·단축키를 바꾼 화면을 다시 보내 총 **200개 추가 화면**이 중복 입력을 일으키지 않는지 확인했다. 엔진 RSS 표본은 시작 21,536KB, 100건 36,976KB, 1,000건 35,008KB였다. 메모리 누수나 최대 처리량의 일반적인 한계를 확정한 검사는 아니다.
- 실행 중 읽기 전용 SQLite 대조에서 전체 **1,118건**, 서로 다른 요청 **1,118건**, `승인 입력 전달` **1,118건**을 확인했다. 이는 최근 200건 스냅샷과 별도로 기록이 저장되는지 확인한 결과다. 종료 직전 전체 DB를 백업·대조한 결과는 아니다.
- 최초 목표는 10,000건이었으나 사용자가 1,000건으로 범위를 줄였다. 별도 테스트 엔진을 일시정지한 시점까지 **1,181건**이 처리됐고 중복·잘못된 입력은 0건이었다. 그 다음 1,182번째 요청은 의도적으로 일시정지했으므로 12초 후 테스트 제한 시간에 도달했다. 원본 `.runtime/stress-0.2.14-single-10000.json`의 `passed: false`, `timeouts: 1`, `missing: 8819`는 이 수동 중단을 포함한다. 10,000건 통과나 1,182번째 요청의 제품 결함을 뜻하지 않는다.
- 별도 20건 검사로 정해진 횟수에서 정상 종료하는 경로와 SQLite 전체 건수·서로 다른 요청·전달 결과 **20/20/20**을 검증했다. 재현 스크립트와 `--stress`, `--stress-terminals`, `--stress-report` 사용법을 추가했다. 제품 코드·배포 버전은 변경하지 않았다.
- 범위는 **실제 격리 PTY + 0.2.14 배포 엔진 + 모의 VS Code 연결**이다. 사용자 Terminal에 입력을 보내거나 실제 Codex 모델 호출을 실행하지 않았다. 구형 macOS의 실제 Terminal 승인 성공률, Claude 연속 승인 및 여러 터미널의 동시 부하는 이번 1,000회 결과에 포함하지 않는다.

## 0.2.14 Codex 연속 승인 누락

- 제공된 캡처의 `Environment: local`, `rg -n` 명령, Yes / 반복 허가 (p) / No (esc) 문구를 그대로 검사했다. 문구 감지와 JavaScriptCore 모의 Terminal의 최종 원문 검증은 기존 구현에서도 통과했다.
- 배포된 0.2.13 바이너리에 `node scripts/screen-integration-check.mjs dist/AutoApprove.app/Contents/MacOS/autoapprove --codex-only`를 실행했다. 첫 승인은 전달됐지만 같은 프로세스의 화면이 곧바로 캡처의 다음 명령으로 바뀌면 `next Codex permission without an intervening working frame`에서 시간 초과했다. 두 번째 요청은 `approval`, `automatic: true`, `pendingInTerminal: false` 상태에 남았다. 이전 요청의 `attempted`가 새 요청에 복사되는 경로를 확인했다.
- 수정본은 Codex 요청 내용이 바뀌면 전송 상태·재시도 횟수를 새로 시작한다. 줄바꿈·공백·단축키·이전 출력 변화는 중복 입력하지 않는다. 식별에 전체 요청을 사용하고 최종 원문 비교는 유지한다. 이전 요청의 늦은 응답은 다음 요청의 전송 상태를 변경하지 않으며 같은 요청의 재표시에는 확인 필요 상태가 유지된다.
- 릴리스 빌드, 핵심 검사 **67/67**, 수정본의 **Codex 전용 PTY 연속 승인 검사**, 기존 PTY 화면 검사, 훅/소켓 통합 검사가 통과했다. 연속된 서로 다른 명령, 캡처 원문, 같은 요청의 재표시, 다른 터미널, 일시정지·재개, 일반 3지 작업 질문, 늦은 미전달 응답, 이전 요청의 재시도 한도 소진 후 다음 요청을 포함한다. 4,000자 요약 밖에서만 명령이 달라도 구분한다.
- 실행 앱을 **0.2.14 (17)**로 교체했다. 활성 **10개**, 자동 승인 **9개**, 동일 세션의 설정 변경 **0개**이며 순서·일시정지·Terminal 연결 설정을 유지했다. UI 코드는 바뀌지 않아 이번에는 새 화면 크기·레이아웃 검사를 수행하지 않았다. 사용자 Terminal에는 시험 입력을 보내지 않았고, 구형 Mac 실기 검증은 남아 있다.
- 최종 DMG의 코드 서명·체크섬·읽기 전용 마운트·Applications 링크·설치 안내를 확인하고 정상 추출했다. SHA-256: `3c474bc8f30cc1b1ec2ba35096e16299f8cdba30f0da31a02e1c1284d9c461cf`. macOS 14 이상 Apple Silicon용이며 내장 VS Code 확장은 0.2.3이다.

## 0.2.13 승인 대기 복구·백그라운드 모니터링

- Claude·Codex 승인 화면의 마지막 No 선택지와 키 안내가 줄바꿈되면 감지하지 못하던 경로를 수정했다. 첫 Yes·반복 허가·No와 안내의 줄바꿈, CRLF, 변경된 현재 선택, 후속 출력·입력창, 서로 다른 작업 선택지를 검사했다. 검증용 원문은 보존한다.
- VS Code 승인 응답에 8초 제한을 두어 연결이 살아 있어도 결과가 오지 않으면 같은 승인 내역을 `전달 확인 시간 초과 · 터미널 확인 필요`로 갱신하고 알림 대상으로 전환한다. Terminal·VS Code에서 전달 후 같은 요청이 새 화면에서 계속 확인되면 8초 뒤 상세 사유와 확인 대상을 표시한다. 불확실한 입력은 반복하지 않고, 미전달이 확인된 입력의 최대 3회 재시도는 유지한다. 늦은 응답·다른 세대·후속 작업은 분리한다.
- 다음 지시를 받을 수 있는 CLI에 백그라운드 작업만 남으면 `대기 중 · 모니터링`으로 표시한다. Claude의 백그라운드·예약 작업을 포함한 Stop도 대기에 포함한다. 출력이 변하는 모니터, 일반 대기, 새 작업, 질문, 연결 해제, 종료, 이전 스냅샷 읽기와 완료 알림의 분리를 검증했다.
- 릴리스 빌드, 핵심 검사 **66/66**, 훅/소켓 통합 검사와 실제 격리 PTY 화면 통합 검사가 통과했다. PTY 검사는 줄바꿈된 3지 승인, 모니터 출력 변화, 명시적 미전달 재시도, 응답 누락·늦은 응답·동일 화면 지속·후속 작업 보호를 포함한다. 사용자 터미널에 시험 입력을 보내지 않았다.
- `$ui-design-workflow`와 Computer Use로 생산 SwiftUI를 사용하는 격리 앱의 **1040×700**, **784×612** 창을 확인했다. 일반 대기와 모니터링 행·상세·안테나 표시, 대기 필터의 2개 중 모니터링 1개 포함, 대기 수의 접근성 도움말과 줄바꿈을 확인했다. 선택 행의 상태 글자는 시스템 기본 전경색을 사용해 대비를 보완했다. [기본 창](dist/qa/monitoring-0.2.13-desktop.png), [작은 창](dist/qa/monitoring-0.2.13-compact.png). 알림 센터 클릭·사용자 Terminal 입력·전체 VoiceOver·구형 macOS 실행은 이번에 검증하지 않았다.
- 앱 **0.2.13 (16)** 패키징 후 실행했다. 활성 **10개**, 자동 승인 **9개**, 동일 세션의 설정 변경 **0개**이며 기존 순서·일시정지·Terminal 연결 설정을 유지했다. 내장 VS Code 확장은 **0.2.3**이다.
- 최종 DMG의 코드 서명·체크섬·읽기 전용 마운트·Applications 링크·설치 안내를 검사하고 정상 추출했다. `AutoApprove-0.2.13-macOS-arm64.dmg`의 SHA-256은 `058f028d736a1e1b253fc351681a2f2f7e8c781185a0a0950ea9814de74da0cb`이다. 배포 대상은 macOS 14 이상 Apple Silicon이다.
- 0.2.13 검증 당시에는 다른 머신의 macOS·앱 버전과 멈춘 선택지 원문이 제공되지 않았다. 위 실패 경로를 재현·수정한 결과이며 구형 macOS 자체가 원인이라고 확인하거나 해당 머신의 문제가 해결됐다고 검증한 것은 아니다.

## 0.2.12 Yes 변형·단축키·툴팁

- Claude Code·Codex 승인 화면과 Claude 단일 훅 질문에서 같은 허가의 반복 범위만 다른 3개 이상 선택지를 인식한다. 일반 Yes를 우선하며 `don't ask again`/`don’t`, 세션 내 허용, `y / a / esc`·`y / p / esc`, 괄호·대괄호, 추천 표시와 줄바꿈을 검사했다. 원래 답 라벨과 검증용 화면 원문을 보존하고 서로 다른 작업·모호한 Yes·다중 질문·다중 선택·다른 현재 선택·이전 화면은 자동 처리하지 않는다.
- 릴리스 빌드, 핵심 검사 **61/61**, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove`가 통과했다. 훅/소켓 검사에서 반복 허가가 첫 항목인 질문도 이번 요청의 원래 Yes 라벨로 답하고 중복 이벤트에 재응답하지 않았다. 실제 PTY 검사에 Codex의 줄바꿈된 `don’t ask again (a)`와 Claude의 `don't ask again`을 포함했다. 사용자 터미널에 시험 입력을 보내지 않았다.
- `$ui-design-workflow`를 적용해 기존 SwiftUI `.help`를 보완했다. 격리 앱의 실제 **1040×700**, **784×612** 관리 창에서 선택지 체크·직접 입력·전송 버튼 활성화·모의 응답 접수·질문 정리를 확인했다. 접근성 Help에서 체크 전후 문구, 빈 답변·연결 끊김·미설치·페이지 끝의 비활성 이유를 확인했다. **600×660** 연결 설정과 작은 승인 내역 창도 확인했다. [기본 창](dist/qa/tooltips-0.2.12-desktop.jpg), [작은 창](dist/qa/tooltips-0.2.12-compact.jpg), [설정](dist/qa/tooltips-0.2.12-settings.jpg), [내역](dist/qa/tooltips-0.2.12-history.jpg). 검증 질문은 종료 전에 정리했다.
- Computer Use에 hover 전용 동작이 없어 모든 툴팁 팝업의 실제 표시를 확인한 것은 아니다. 네이티브 툴바·메뉴는 접근성 트리에 Help가 노출되지 않아 코드 연결과 열기 동작까지만 확인했다. 전체 VoiceOver, 사용자 Terminal·알림 센터의 직접 입력은 검사하지 않았다. 브라우저·모바일 뷰포트는 네이티브 macOS 앱에 해당하지 않는다.
- 앱 **0.2.12 (15)**를 패키징·엄격한 코드 서명 검증 후 실행했다. 활성 **10개**, 자동 승인 **9개**, 동일 세션의 설정 변경 **0개**이며 사용자가 정한 순서·일시정지·Terminal 연결 설정을 보존했다. [실행 앱](dist/qa/tooltips-0.2.12-production.jpg). 내장 VS Code 확장은 **0.2.3**을 유지한다.
- 최종 DMG의 체크섬·읽기 전용 마운트·포함 앱 서명·Applications 링크·설치 안내를 검증하고 정상 추출했다. `AutoApprove-0.2.12-macOS-arm64.dmg`의 SHA-256은 `21d48261069940d79c6b747295e7274f5565c04014ca48e2578ad602929f234c`이다. DMG와 `.dmg.sha256`을 [v0.2.12 Release](https://github.com/newdlops/autoapprove/releases/tag/v0.2.12)의 배포 파일로 사용한다.

## 0.2.11 터미널 순서 저장·이동

- 기존 SwiftUI `List` / `ForEach.onMove`에 순서 저장을 연결하고 우클릭에 위·아래 이동을 추가했다. 현재 보이는 행의 ID와 순서를 확인한 뒤 해당 행이 차지한 자리만 재배치한다. 숨겨진 행·자동 승인·세션 ID·브랜치를 변경하지 않으며, 저장에 성공한 뒤 화면에 반영한다. 기존 설정 DB의 `sessionOrder`에 저장하고 앱 시작 시 복원한다.
- 릴리스 빌드, 핵심 검사 **58/58**, 훅/소켓 통합 검사와 실제 PTY 화면 통합 검사가 통과했다. 새 검사는 위·아래·맨 끝·여러 항목·제자리 이동, 필터의 숨은 위치 보존, 주기적 발견과 일부 세션부터 시작하는 재실행, 같은 폴더의 별도 세션, 새 세션·종료·폴더 변경, 오래되거나 잘못된 이동 요청, 손상·중복된 저장값과 저장 실패 시 원래 순서 보존을 포함한다.
- `$ui-design-workflow`를 적용했다. 생산 UI를 사용하는 격리 앱에서 우클릭 이동 후 선택 유지, 검색된 두 행만 이동한 뒤 숨은 행의 위치 보존, 맨 위 항목의 위로 이동 비활성, 스위치 독립 동작, 재배치된 행의 정확한 더블클릭 콜백을 확인했다. **1040×700**, **784×612**에서 안내 문구·선택 행·상세 배치를 확인했다. [기본 창](dist/qa/reorder-desktop.jpg), [작은 창](dist/qa/reorder-compact.jpg). 검증 질문은 종료 전에 정리했다.
- 직접 사용 확인: 0.2.11 실행 후 사용자가 터미널 행을 마우스로 드래그하면 순서가 바뀐다고 확인했다. 이는 사용자 확인이며 자동화 도구로 재현한 결과는 아니다. Computer Use에서는 생산 목록과 별도의 기본 `List.onMove` 예제 모두 드래그 재배치가 되지 않아, 자동화 검증은 같은 저장 경로를 사용하는 우클릭 이동으로 수행했다. 임시 입력 진단 코드와 기본 예제는 제거했다. 전체 VoiceOver와 실제 사용자 Terminal 열기 입력은 이번 범위에 포함하지 않았다.
- 앱 **0.2.11 (14)** 패키징·엄격한 코드 서명 검증 후 실행했다. 활성 **10개**, 자동 승인 **9개**, 동일 세션의 설정 변경 **0개**이며 기존 목록 순서·일시정지·Terminal 연결 설정을 유지했다. [실행 앱](dist/qa/reorder-production.jpg).
- 내장 VS Code 확장은 **0.2.3**이다. DMG 체크섬·읽기 전용 마운트·포함 앱 코드 서명·앱/Applications 링크/설치 안내 구성을 검사하고 정상 추출했다. `AutoApprove-0.2.11-macOS-arm64.dmg`의 SHA-256은 `d5d0772e20298499e5eb512e01bb490ebdff81733512e03d26ee849773299db8`이다. DMG와 `.dmg.sha256`을 [v0.2.11 Release](https://github.com/newdlops/autoapprove/releases/tag/v0.2.11)의 배포 파일로 사용한다.

## 0.2.10 현재 폴더의 Git 브랜치·DMG

- 목록의 터미널 제목 아래와 상세의 폴더 경로 아래에 현재 Git 브랜치를 추가했다. 긴 이름은 목록에서 가운데를 줄이고 상세에서 줄바꿈하며, 브랜치명·detached HEAD 커밋 검색과 전체 이름 툴팁을 제공한다. 최초 조회·비저장소·읽기 실패를 구분한다. 현재 폴더를 매번 다시 확인하고, 승인 처리와 독립된 배경 작업에서 최대 네 폴더씩 조회한다.
- 릴리스 빌드와 핵심 검사 **54/54**, 훅/소켓 통합 검사, PTY 화면 통합 검사가 통과했다. 임시 실제 Git 저장소에서 최초 커밋 전 브랜치·하위 폴더·연결된 워크트리·브랜치 전환·detached HEAD·비저장소·없는 폴더·상속된 Git 환경 격리를 확인했다. 폴더 변경·종료 후 늦은 결과를 무시하며, 검증용 PTY의 브랜치 전환이 수동 새로고침 없이 반영됨을 확인했다.
- `$ui-design-workflow`를 적용하고 생산 SwiftUI 화면을 사용하는 격리 앱에서 **1040×700**, **784×612**의 긴 이름·조회 중·실패·비저장소·detached HEAD를 확인했다. 브랜치 일부로 검색하면 해당 세션만 남고, 한 번 클릭은 선택만, 더블클릭은 정확한 세션의 모의 열기 콜백을 한 번 호출했다. [긴 브랜치 작은 창](dist/qa/git-branch-long-compact.jpg), [기본 창](dist/qa/git-branch-desktop.jpg), [조회 상태](dist/qa/git-branch-error-desktop.jpg). 검증 질문은 종료 전에 정리했다. 실제 Terminal 열기 입력과 전체 VoiceOver 검사는 이번 변경 범위에 포함하지 않았다.
- 앱 **0.2.10 (13)**를 패키징하고 엄격한 코드 서명 검증 후 실행했다. 실행 중인 **10개 세션**의 표시값을 각 폴더의 실제 Git 결과와 읽기 전용으로 대조해 불일치 **0개**를 확인했다. 기존 자동 승인 **9개**, 동일 세션 설정 변경 **0개**, 전체 일시정지 해제 상태를 보존했다. [실행 중인 앱](dist/qa/git-branch-production.jpg).
- 내장 VS Code 확장은 **0.2.3**이다. `node scripts/package-dmg.mjs`의 DMG 체크섬·읽기 전용 마운트·포함 앱 코드 서명 검증이 통과했으며 앱·Applications 바로가기·설치 안내를 확인하고 정상 추출했다. `AutoApprove-0.2.10-macOS-arm64.dmg`의 SHA-256은 `4d78e92f7b6f4143d86cd0a9641130511e0d48a3bb0b3e3cf5a17cfaeebbfaa3`이다. DMG와 `.dmg.sha256`을 [v0.2.10 Release](https://github.com/newdlops/autoapprove/releases/tag/v0.2.10)의 배포 파일로 사용한다.

## 0.2.9 DMG 배포

- 검증된 앱 **0.2.9 (12)**와 내장 VS Code 확장 **0.2.3**을 `AutoApprove-0.2.9-macOS-arm64.dmg`로 패키징했다. 이전 배포 0.2.7 이후의 완료 알림·Codex 기록 조회 복구·터미널 더블클릭을 함께 포함한다.
- `node scripts/package-dmg.mjs`의 `hdiutil verify`, 읽기 전용 마운트, 포함 앱의 엄격한 코드 서명 검증이 통과했다. 앱·Applications 바로가기·설치 안내 세 항목과 링크 대상·안내 원문 일치를 확인하고 정상 추출했다.
- SHA-256: `74fdd1d5b173b291c0dd2fdd05f49eeb54389fa0da139e5be6ebd0b9cafc0be9`. DMG와 `.dmg.sha256`을 [v0.2.9 Release](https://github.com/newdlops/autoapprove/releases/tag/v0.2.9)의 배포 파일로 사용한다. 소스는 같은 버전 태그로 고정한다. 아래 기능 검증은 해당 앱 빌드에서 수행했다.

## 0.2.9 Codex 기록 조회 복구·터미널 더블클릭

- 격리된 SQLite DB에 `BEGIN EXCLUSIVE` 잠금을 걸어 기존 질문 조회 오류와 ‘작업 완료 기록 형식을 지원하지 않습니다’가 함께 발생하는 것을 재현했다. 잠금을 풀면 같은 형식을 정상 조회했다. 실제 imgflash 세션의 스키마도 지원 형식이며 재확인 때 정상 조회됐다. 사용자 스크린샷 시점의 원시 SQLite 오류 코드는 확보하지 못했으므로 당시 원인이 반드시 잠금이었다고 단정하지 않는다.
- SQLite BUSY/LOCKED/SCHEMA, 기록 준비 중, 실제 테이블·열 누락, 파일 접근 실패와 잘못된 내용을 구분한다. 일시적 오류는 첫 두 조회까지 경고 없이 다시 확인하고 세 번 연속 실패하면 정확한 안내를 표시한다. 실패한 조회는 빈 목록과 구분해 기존 질문·질문 알림·증분 커서를 보존한다. 질문과 완료 조회는 각각 복구하며 확인되지 않은 완료 상태를 알림으로 보내지 않는다. 실제 스키마 오류는 즉시 표시하고 정상 조회가 되면 오류를 해제한다.
- 릴리스 빌드, 핵심 검사 **51/51**, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과. 새 검증은 실제 SQLite 잠금·해제, 잠금 중 새 질문 기록의 복구, 기존 질문 알림·관찰 시각 보존, 성공한 빈 대기열 반영, 실제 스키마 오류의 즉시 표시, 질문/완료 오류 분리, 읽기 전용으로 없는 DB를 생성하지 않는 동작을 포함한다.
- `$ui-design-workflow`를 적용하고 기존 SwiftUI 목록·선택·스위치·터미널 강조 경로를 유지했다. 생산 `SessionWindow`를 사용하는 격리 앱에서 한 번 클릭은 선택만, 더블클릭은 정확한 세션의 열기 콜백 1회, 다중 선택 중에는 클릭한 세션만 전달됨을 확인했다. 검색 후 대상 식별, 우클릭 열기, 자동 승인 스위치의 독립 동작, 연결 전 안내, **999×670** 및 **784×612**의 긴 제목·질문·버튼 배치와 작은 창 더블클릭을 확인했다. [기본 창](dist/qa/double-click-desktop.jpg), [작은 창](dist/qa/double-click-compact.jpg). 검증 질문은 종료 전 목록에서 정리했다.
- 앱 **0.2.9 (12)**를 패키징하고 `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과 후 실행했다. 재시작 직후 세 차례 상태 조회에서 활성 **9개**, 자동 승인 **8개**, Terminal 연결 **8개**, 기존 세션 설정 변경 **0개**, 전체 일시정지 꺼짐을 확인했다. Codex 두 세션의 질문·완료 오류는 없고 관찰 시각이 갱신됐다. 실제 앱의 imgflash 상세에서도 두 주황색 기록 오류가 사라진 것을 확인했다. [imgflash 상세](dist/qa/0.2.9-imgflash.jpg).
- 범위: 더블클릭의 UI 입력·정확한 세션 전달은 실제 네이티브 검증 앱에서 확인했지만, Computer Use가 Terminal 접근을 차단하므로 실제 사용자 Terminal 창 이동은 이번에 직접 실행하지 않았다. 기존 이동 코드의 정확한 TTY·최소화 복원·닫힌 탭 검사는 통과했다. 전체 VoiceOver 및 모든 macOS 버전은 별도 검증하지 않았으며, 웹·모바일 화면 검증은 해당하지 않는다.

## 0.2.8 최종 응답 완료 알림

- Claude 루트 `Stop`과 Codex 최신 루트 턴의 `completed`를 기존 macOS 알림 경로에 추가했다. 프로젝트·도구·최종 응답 요약을 표시하며 알림 클릭은 기존 세션 ID 검증·터미널 이동·강조를 사용한다. 자동 승인 설정·일시정지와 독립적이다. 3초 확인 중 새 작업이 관찰되면 전송을 취소한다.
- 최초 관찰의 과거 완료, 단순 idle, 도구 하나·하위 에이전트 종료, Codex failed/interrupted, 세션 종료는 완료 알림을 만들지 않는다. Claude 훅이 보고하는 백그라운드·예약 작업과 미응답 질문도 제외한다. Codex 완료 조회는 질문 수집과 오류를 분리하고 기존 읽기 전용 루트 매핑을 재사용한다. 실제 설치된 Codex 0.155.1의 `thread_turns` 상태·초 단위 시간·최종 응답 연결 스키마를 확인했다.
- `swift build --disable-sandbox --cache-path .build/cache -c release`, 핵심 검사 **48/48**, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과. 새 검증은 첫 기준선, 짧은 턴, 중복 Stop/완료, 작업 재개, 백그라운드·예약·질문 보존, 읽기 오류 복구, 최신 턴과 정확한 최종 응답 연결, 프로세스 종료 후 늦은 결과를 포함한다. helper→소켓→상태에서 실제 Stop JSON과 새 작업의 완료 상태 제거도 검사했다.
- 격리된 네이티브 앱에서 실제 `UNUserNotificationCenter` 전달 내역을 확인했다. 기존 질문 알림 2개와 공존하면서 초기 완료 **0개 → Claude 완료 1개 → Codex까지 완료 2개**, 같은 완료 반복 후 **2개 유지**, 새 작업과 완료 직후 0.5초 재개 후 **완료 0개·예약 0개**를 확인했다. 종료 전 검증 질문도 정리해 검증 앱 알림을 **0개**로 만들었다. [전달 결과](dist/qa/completion-delivered.jpg), [재개·취소 결과](dist/qa/completion-resumed.jpg). 검증용 이벤트만 사용했으며 사용자 대화에 시험 응답을 보내지 않았다.
- `$ui-design-workflow`를 적용해 기존 설정·폰트·상태색·툴팁을 유지했다. **600×660** 설정에서 새 안내 줄바꿈·허용 상태·시스템 설정 버튼과 아래 연결 항목까지 스크롤을 확인했다. 검증 창 캡처는 **1040×672**이다. [알림 설정](dist/qa/completion-settings.jpg). 전체 VoiceOver·키보드 흐름, 오류/권한 거부 상태의 새 화면은 이번에 별도로 시각 검증하지 않았다. 네이티브 앱이므로 웹·모바일 검증은 해당하지 않는다.
- 앱 **0.2.8 (11)** 패키징·`codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과 후 실행했다. 활성 **9개**, Terminal 연결 **8개**, 자동 승인 **8개**, 일시정지 해제와 기존 세션별 자동 승인 설정 일치를 확인했다. 실제 Codex 두 세션의 질문·완료 기록 읽기 오류는 없고 재실행 직후 과거 완료 알림 후보는 0개였다. 실제 앱 알림 권한은 **허용됨**이다.
- 이번 검증은 실제 사용자 TUI의 최종 응답→배너 클릭→터미널 이동 전체를 검증한 것은 아니다. Computer Use의 Terminal·알림 센터 접근 제한을 우회하지 않았다. Claude의 이전 버전에서 생략되는 백그라운드 정보나 다른 훅의 지연된 재개에는 감지 한계가 있다. 완료는 응답 종료이며 의미상 목표 달성이나 모든 명령의 성공을 보증하지 않는다.

## 0.2.7 DMG 배포

- release 빌드 스크립트로 앱 **0.2.7 (10)**과 내장 VS Code 확장 **0.2.3**을 생성했다. 앱·helper 모두 **arm64**, Mach-O의 최소 macOS 버전 **14.0**을 확인했다. 확장에는 `@xterm/headless 5.5.0`의 저작권·MIT 라이선스 안내를 포함했다.
- `node scripts/package-dmg.mjs`로 `AutoApprove-0.2.7-macOS-arm64.dmg`와 `.dmg.sha256`을 생성했다. 앱·Applications 바로가기·설치 안내만 포함하며 사용자 설정·승인 내역·검증 스크린샷은 포함하지 않는다.
- `hdiutil verify`, 읽기 전용 마운트, 마운트된 앱의 `codesign --verify --deep --strict` 통과. 이미지의 세 항목과 Applications 링크 대상, 설치 안내가 소스와 일치함을 확인하고 정상 추출했다. 자동화 스크립트 구문과 `git diff --check`도 통과했다.
- Developer ID 인증서가 없어 공증하지 않았다. 새로 다운로드한 파일의 Gatekeeper 허용 흐름·다른 Mac에서의 설치는 검증하지 않았으며 설치 안내와 Release에 서명 상태를 명시한다. Finder 창 배치 등 새 설치 UI는 추가하지 않았다.

## 0.2.7 앱 내 Codex 질문 응답

- 앱 **0.2.7 (10)** 패키징·엄격한 코드 서명 검증 후 재실행했다. 활성 9개·Terminal 연결 8개·자동 승인 8개·일시정지 해제와 각 활성 세션의 기존 자동 승인 설정을 보존했다. 최종 실제 앱 조회 시 두 Codex 세션은 수집 오류 없이 질문 0건으로 표시됐으며 입력 폼 검증은 아래 격리 검증 앱에서 수행했다. 사용자 대화에 시험 답변을 보내지는 않았다. [최종 앱](dist/qa/reply-production.jpg).

- Codex 비동기 질문에 선택지 체크박스·복수 선택·직접 답변·추가 설명·명시적인 ‘답변 보내기’를 제공한다. 답변이 비면 전송할 수 없고 질문별 입력 상태를 유지한다. ‘목록에서 정리’와 실제 답변 전송을 구분한다. Claude의 반환된 질문과 실행 권한 화면은 해당 요청 아래의 ‘터미널에서 답하기’로 기존 정확한 세션 이동·강조 경로를 사용한다.
- 전송 전 실제 프로세스 시작 시각·TTY와 유일한 루트 thread ID, 아직 남아 있는 정확한 질문을 다시 검증한다. 실행 중인 Codex 바이너리를 `proc_pidpath`로 찾고 해당 CODEX_HOME에서 `queue --thread UUID --message TEXT`를 직접 인자 배열로 실행한다. 셸 해석 없이 질문 인용·선택지 원문·직접 입력을 전달한다. 공유 daemon 시작이나 사용자 터미널 시험 입력은 없다.
- 접수된 답변은 **전달 대기**이며 현재 턴이 실제 수신했다는 주장은 하지 않는다. 사전 검증 실패는 재시도할 수 있고, 실행 후 타임아웃·불명확한 접수 결과는 확인 필요로 남겨 중복 전송을 차단한다. 전송 전에 감사 기록과 지속 예약 상태를 저장한다. 전송 중 앱 종료 또는 재실행에도 무조건 다시 보내지 않는다. 자동 승인 설정과 별개의 명시적 수동 응답이다.
- `swift build --disable-sandbox --cache-path .build/cache -c release`, **43/43** 핵심 검사, 기존 훅/소켓 통합 검사와 PTY 화면 통합 검사 통과. 추가 범위: 복수 선택·직접 입력의 정확한 조합, 여러 줄·셸 특수 문자·빈 값·NUL, 다른 thread 접수 거부, 전송 전 기록·예약, 같은 질문 재전송 및 재시작 차단, 결과 미확인과 중간 종료 복원, 잘못된 대화/감사 기록 실패 시 입력 미전달, 전달 대기 기록 필터.
- `node scripts/codex-queue-integration-check.mjs` 통과. 임시 CODEX_HOME의 합성 세션에 설치된 **Codex 0.155.1** CLI로 메시지를 등록하고 공식 `thread/queue/list`에서 정확한 큐 ID·대화 ID·여러 줄 답변과 셸 특수 문자 보존을 확인했다. 모델 턴·외부 API 호출 없이 수행하며 기존 사용자 대화에 답변을 보내지 않는다. 실제 사용자의 실행 중인 TUI가 이 답변을 소비하는 시점은 검증하지 않았다.
- 생산 엔진과 UI를 사용하는 격리 검증 앱에서 **902×612**의 복수 체크박스 선택·추가 한글 답변 입력·빈 답변 버튼 비활성·전송 클릭, **1040×700**의 접수 결과와 남은 질문 건수, **784×612**의 긴 질문·결과 미확인 안내를 확인했다. 검증 앱의 전송 경로는 성공/불명확 응답을 주는 모의 수신자이며 사용자 터미널을 열지 않는다. [선택과 직접 입력](dist/qa/reply-choices.jpg), [접수 상태](dist/qa/reply-sent-desktop.jpg), [전송 결과 기록](dist/qa/reply-history.jpg). 접근성 트리에서 체크박스·전송 버튼 상태, 실패 후 전송 버튼 제거와 직접 입력한 답변 보존, 저장된 ‘답변 대기열 등록’·‘답변 접수 확인 필요’ 항목을 확인했다. 전체 키보드/VoiceOver 및 실제 Terminal 이동은 이번 검증 범위 밖이다.

## 0.2.6 Codex 질문 대기열·터미널 제목·승인 상세 접기

- 설치된 Codex **0.155.1**의 app-server JSON schema, 로컬 이력 DB 스키마와 질문/답변 이벤트 형식을 확인했다. 기존 CLI에는 연결 가능한 공유 daemon 소켓이 없었다. `queue_1.sqlite`의 사용자 입력 큐와 비동기 질문은 별개다. 실제 프로세스가 열어 둔 유일한 `source: cli` 루트 rollout으로 `thread_history_1.sqlite`를 연결하고 읽기 전용·일관된 트랜잭션·증분 커서로 수집한다. 하위 에이전트·동일 프로젝트의 다른 프로세스·모호한 루트는 섞지 않는다. Codex 설정이나 기록을 수정하지 않는다.
- 질문 대기열은 작업 상태와 독립적이며, 여러 질문의 ID·선택지를 유지한다. 정확한 질문 인용과 답변을 유일하게 연결한 항목만 해제하고 일반 입력·도구 완료로 없애지 않는다. 미응답 상태 API를 읽는 기능은 아니다. 인용 없는 답변·취소·같은 제목의 중복 질문은 남을 수 있어 로컬 ‘확인 완료’를 제공하고 재실행 후에도 유지한다. 읽기 실패에는 마지막 목록을 보존한다. 목록의 건수·확인 필요 필터·메뉴 막대·알림 추적을 다중 질문에 적용했다.
- 프로젝트명 다음 줄에 Terminal 사용자 지정 탭 제목 또는 해당 선택 탭의 창 제목, VS Code 등록 이름을 표시한다. 제목 검색·전체 제목 툴팁을 제공한다. 선택되지 않은 탭에는 다른 탭의 창 제목을 붙이지 않는다. 선택적 제목 조회 실패는 화면 읽기나 승인을 막지 않는다. 기존 VS Code 확장 0.2.3의 이름 전달을 사용하며 확장 변경은 없다.
- 최근 승인 기록에 네이티브 DisclosureGroup을 적용했다. 결과 문구/화살표로 전체 요청·선택한 답·출처를 펼치고 다시 접으며 각 항목은 독립적이다. 마우스로 결과 문구를 눌러 펼침/접힘과 여러 항목 동시 펼침을 확인했다. 접근성 트리의 disclosure triangle on/off도 확인했다. 키보드만으로 완주하는 흐름과 VoiceOver는 이번 검증에 포함하지 않았다.
- Release 빌드와 **39/39** 핵심 검사, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과. 추가 검사: 질문별/부분 답변·일반 메시지·인용만 있는 입력·모호한 동일 제목, 읽기 전용 DB·증분 갱신·재실행·미지원 스키마, 정확한 루트 매핑, 동시 알림·화면 갱신·일시 오류·로컬 정리 유지·종료 후 늦은 결과, Terminal 제목 계약과 VS Code 이름 갱신. 사용자 터미널에 시험 입력을 보내지 않았다.
- `$ui-design-workflow`와 Impeccable의 기존 UI 보존·마무리 지침을 적용했다. 격리된 네이티브 검증 앱에서 **1040×700**, **784×612**의 긴 제목, 두 질문/긴 선택지, 질문 한 건 정리 후 다른 건 유지, 제목 검색·검색 결과 없음·확인 필요 필터, 40줄 전체 요청·답변 펼침과 스크롤을 확인했다. [기본 질문 목록](dist/qa/queue-desktop.jpg), [최소 창 질문 목록](dist/qa/queue-minimum.jpg), [승인 상세](dist/qa/approval-expanded.jpg), [최소 창 승인 상세](dist/qa/approval-expanded-minimum.jpg). 선택 행에서 질문 수가 잘 보이도록 시스템 전경색을 사용했다. 브라우저·모바일 검증은 네이티브 macOS 앱에 해당하지 않는다. 전체 Impeccable 독립 이중 평가나 웹 감사는 수행하지 않았다.
- 앱 **0.2.6 (9)** 패키징 및 `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과 후 실행했다. 실제 활성 **9개**, Terminal **8개 연결**, 자동 승인 **8개**와 일시정지 해제 상태를 확인했다. 살아 있는 동일 세션의 기존 자동 승인 설정은 모두 보존됐으며 검증 PTY의 종료된 임시 행 3개는 재실행 목록에서 사라졌다. 실제 **8개 터미널 제목**, Codex 두 루트 세션의 기록상 미확인 질문 **2건/12건** 수집을 확인했다. 이는 기록 기반 미확인 건수이며 실제 TUI 큐와 일대일 대조한 수치는 아니다. [최종 앱](dist/qa/queue-production.jpg).
- 사용자가 허용한 알림 권한은 최종 앱 설정에서 **허용됨**으로 확인됐다. [최종 알림 설정](dist/qa/queue-notifications-allowed.jpg). 이번에도 실제 Terminal/알림 센터의 클릭→이동 검증은 Computer Use 접근 제한으로 수행하지 않았다. 질문별 알림 추적 로직과 기존 클릭 시 세션 검증·강조 경로는 유지한다.

## 0.2.5 예·아니오 판별 확장·응답 알림

- Claude 훅 질문 판별을 질문 문구 대신 명확한 두 개의 예·아니오 선택지로 변경했다. 사용자 제공 ‘미커밋·브랜치 현황을 먼자 훑어볼까요?’를 실제 helper→소켓→응답 JSON으로 검사했다. 순서가 바뀐 선택지, 추천 표시, 쉼표·대시 뒤 설명, 한국어 정규화를 지원하고 원래 답 라벨을 보존한다. 세 가지 작업 선택, 다중 질문·다중 선택, 기존 답, 모호한 선택지는 자동으로 답하지 않는다.
- 질문 후 Claude의 `permission_prompt`·`idle_prompt`가 와도 미응답 질문과 선택지를 유지한다. 자동 처리할 수 없는 질문·수동 확인 요청에는 프로젝트·도구·질문을 담은 macOS 알림을 추가했다. 반복 관찰과 같은 도구 요청은 중복 알림을 막고 해결·종료 시 정리한다. 자동 처리 중인 권한 요청과 단순 입력창 타이핑은 제외한다.
- 알림 클릭과 ‘터미널 열기’는 동일한 터미널 이동·강조 코드를 사용한다. 클릭 시 세션 ID를 확인하고 살아 있는 PID·시작 시각·TTY·도구를 다시 검사한다. 같은 프로젝트·재사용된 TTY를 다른 세션의 이동 대상으로 쓰지 않는다. 앱이 종료된 상태에서 알림으로 실행되면 최초 탐색 완료를 기다린다. 연결 없음·종료·권한 오류는 오류 창으로 알린다.
- `swift build --disable-sandbox --cache-path .build/cache -c release`, `.build/release/autoapprove-checks` **34/34**, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과. 알림 회귀 범위는 삼지선다, 반복 관찰, 질문 해결, 같은 내용의 새 질문, 다른 tool_use_id, 일시정지·자동 승인 끄기, 입력 전달 실패, 잘못된 이동 대상·종료·연결 없는 VS Code이다. 사용자 터미널에 시험 입력을 보내지 않았다.
- 격리된 검증 앱에서 생산 `QuestionNotifications`를 사용해 macOS에 실제 알림이 전달된 것(1개), 같은 질문 재관찰 후 1개 유지, 질문 해결 후 0개 제거를 시스템 API로 확인했다. 검증 창의 수신 카운터에 **알림 클릭 1회**도 관측했다. 알림 클릭 순간의 배너와 강조 표시를 연속으로 시각 관찰한 것은 아니며, 검증 앱의 이동 대상은 자체 창이다. 실제 사용자 Terminal/VS Code로의 알림 이동은 검증하지 못했다. Computer Use가 `com.apple.UserNotificationCenter`와 Terminal 접근을 제한하며 이를 다른 도구로 우회하지 않았다.
- `$ui-design-workflow`를 적용해 기존 SwiftUI 구성·시스템 글꼴·SF Symbols·의미 색상을 유지했다. 실제 앱 **1226×722**, **784×612**에서 설명의 줄바꿈·긴 기존 질문·목록/상세 스크롤을 확인했다. [기본 창](dist/qa/notifications-desktop.jpg), [최소 창](dist/qa/notifications-minimum.jpg). **600×660** 설정에서 알림 권한 대기·비활성 허용 버튼·도움말·기존 연결 항목까지의 스크롤을 확인했다. [알림 설정](dist/qa/notifications-settings.jpg), [설정 아래쪽](dist/qa/notifications-settings-bottom.jpg). 네이티브 macOS 앱이므로 웹 뷰포트·React 감사는 해당하지 않는다. 최종 재실행 후 macOS 권한이 비허용 상태로 확인되어 **784×612**에서 [알림 꺼짐 안내](dist/qa/notifications-disabled.jpg)와 [최종 설정](dist/qa/notifications-settings-final.jpg)도 확인했다. 전송 오류 UI·VoiceOver 전체 흐름은 시각 검증하지 않았다.
- 앱 **0.2.5 (8)** 패키징과 `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과. 재실행 후 활성 9개 세션, Terminal 8개 연결, 기존 자동 승인 8개와 전체 일시정지 해제 설정 보존을 확인했다. 최종 알림 권한은 macOS의 비허용 상태로 확인됐다. 시스템 알림 설정에서 AutoApprove 알림 허용을 직접 켜야 하며 앱의 해당 안내 화면을 열어두었다. 권한 설정은 도구로 변경하지 않았다. VS Code 확장은 변경하지 않았고 기존 0.2.3 번들을 유지한다.

## 0.2.4 앱 아이콘·툴팁·Claude 작업 시작 응답

- 내장 이미지 생성 도구로 남색 터미널·흰 프롬프트·민트 체크 아이콘을 만들었다. [원본 PNG](assets/AppIcon.png)와 [생성 프롬프트](assets/README.md)를 저장했다. 원본은 1254×1254 RGBA이며 패키징 과정에서 16–1024px의 표준 macOS 표현과 ICNS를 만든다. 앱 정보 창에서 **0.2.4 (7)** 버전과 실제 번들 아이콘을 확인했다. [앱 정보 창](dist/qa/icon-about.jpg). 64px 파생 이미지의 기호 구분도 육안으로 확인했다. `iconutil`은 샌드박스 안에서 Invalid Iconset을 반환했으나 정상 실행 환경에서 변환됐으며 아이콘을 포함한 최종 코드 서명 검증을 통과했다.
- 관리 창, 연결 설정, 승인 내역, 메뉴에 기본 `.help` 툴팁을 추가했다. 검색·필터·자동 승인·작업 상태·TTY/PID·터미널 이동·결과의 의미를 설명하고 연결 전·연결 중·일시정지·페이지 끝·복사 완료에 따라 문구를 바꾼다. 중요한 자동 응답 범위와 오류 안내는 본문에도 유지한다. 사용자 스위치나 연결 설정을 시험 목적으로 바꾸지 않았다.
- 실제 앱 **1040×700**, **784×612**에서 검색, 세션 상세, 새 자동 응답 설명의 줄바꿈, 연결 설정 열기·닫기와 도움말 연결을 확인했다. [기본 창](dist/qa/tooltips-desktop.jpg), [최소 창](dist/qa/tooltips-minimum.jpg), [연결 설정](dist/qa/tooltips-settings.jpg). 접근성 트리에서 검색·필터·스위치·연결 버튼·비활성 페이지 버튼의 Help 내용을 확인했다. Computer Use에 hover 전용 동작이 없어 모든 툴팁 팝업의 실제 표시를 시각 검증한 것은 아니다. VoiceOver 전체 흐름도 검증하지 않았다.
- 사용자가 지목한 `vcmsecond` 요청은 `AskUserQuestion`의 예·아니오 작업 시작 확인이었다. 이전 구현은 이 도구를 항상 직접 응답 대상으로 남겼다. 단일 예·아니오 작업 시작/계속 확인을 인식해 선택지 위치와 무관하게 ‘예’ 라벨을 `updatedInput.answers`로 전달하도록 추가했다. 원래 질문·선택지와 선택한 답을 기록하며 다중 질문·다중 선택·다른 대안 선택·기존 답을 변경하지 않는다. [공식 훅 응답 형식](https://code.claude.com/docs/en/hooks#pretooluse-decision-control)을 확인했고 설치된 Claude CLI 버전은 **2.1.92**였다. 이미 반환된 질문을 기록으로 재생하거나 사용자 터미널에 테스트 입력을 보내지 않았다.
- Release 빌드 및 핵심 검사 **31/31**, 훅/소켓 통합 검사, 테스트 PTY 통합 검사 통과. 추가 범위: 한국어 정규화, 순서가 바뀐 예/아니오, 정확한 선택지 문자열과 원래 질문 보존, 무관한 질문 제외, PreToolUse/PermissionRequest의 응답 위치, 같은 tool_use_id 중복 방지, 세션별 켜짐·전체 일시정지·세션 격리, 저장 실패 시 응답 차단, 답의 영구 기록과 결과 필터. 실제 helper stdin→소켓→JSON stdout으로 질문 응답을 왕복 검사했다. 새 질문에 대한 실제 Claude UI의 수락·작업 재개는 아직 검증하지 않았다.
- 별도 검증 앱의 합성 질문에서 검색→상세 선택, `선택한 답: 예`, 질문 내용·프로젝트·결과를 **784×612 라이트·다크**로 확인했다. [질문 응답 내역](dist/qa/start-answer-audit.jpg), [다크 모드](dist/qa/start-answer-audit-dark.jpg). 검증 기록은 사용자 데이터베이스에 넣지 않았다.
- 최종 앱 재실행 후 Terminal **8개 연결**, 활성 Claude/Codex **9개**, 재실행 전 켜져 있던 자동 승인 **8개 설정**, 일시정지 해제와 기존 기록 **14건** 보존을 확인했다. Claude의 새 작업 이벤트도 수신했다. 사용자가 QA 도중 창 크기와 선택을 바꾼 후에는 해당 사용자 상태를 유지했다. VS Code 확장은 이번 변경 대상이 아니며 번들에는 기존 **0.2.3** 확장을 포함했다.

## 0.2.3 터미널 강조·승인 내역

- `터미널 열기`는 정확한 TTY의 탭을 선택하고 최소화된 창을 복원한 뒤, 해당 창 위치에 3초 동안 파란 테두리와 프로젝트·도구·TTY를 표시한다. 강조 패널은 키보드 포커스를 받지 않고 마우스 입력을 통과시키도록 구현했다. VS Code 확장은 대상 터미널을 연 뒤 이름이 포함된 알림과 3초 상태 표시를 제공한다.
- 전체 `승인 내역` 창을 추가했다. 세션 종료·앱 재실행 이후에도 요청 내용, 시각, 프로젝트 경로, 도구, PID·TTY, 전달 결과를 조회한다. 검색, 결과 필터, 100건 단위 페이지, 요청 복사를 제공한다. SQLite의 기존 2,000건 자동 삭제 제한을 제거했으며 기존 기록의 누락된 프로젝트 정보를 임의로 복원하지 않는다.
- 화면 승인 시도를 입력 전에 저장하고 같은 기록의 결과를 갱신한다. 저장 실패 시 입력을 보내지 않고 앱에 오류를 표시한다. 연결이 끊기거나 입력 결과가 불명확하면 성공으로 표시하지 않는다. `승인 전달`은 응답 전달 기록이며 명령 실행 성공을 뜻하지 않는다.
- Release 빌드와 핵심 검사 **27/27**, VS Code TypeScript·화면 모듈 검사 **5/5**, 훅/소켓 통합 검사, 테스트 PTY 통합 검사를 통과했다. 추가 검증은 2,006건 보존·재열기, 페이지 중복 방지, 검색 이스케이프, 결과 필터, 이전 JSON 호환, 긴 훅 입력과 세션 종료 후 메타데이터 보존, 시도·응답의 단일 기록 갱신이다. Terminal 이동은 JavaScriptCore 모의 객체로 정확한 탭·최소화 복원·닫힌 탭·권한 거부를 검사했고, 다중 화면 좌표 변환을 단위 검사했다.
- 별도 검증 앱에 합성 기록 125건을 넣어 실제 네이티브 렌더링을 확인했다. **1040×700**에서 긴 요청, 2페이지 이동과 선택, 검색 결과 없음, **784×612**에서 줄바꿈과 결과 필터를 확인했다. 필터는 42건, 프로젝트 검색과 조합하면 21건을 반환했다. 페이지 이동 시 이전 스크롤 위치가 남는 문제를 수정하고 맨 위에서 새 페이지가 시작되는 것을 재확인했다. [기본 창](dist/qa/audit-desktop.jpg), [2페이지](dist/qa/audit-page-two.jpg), [작은 창·필터](dist/qa/audit-minimum-filter.jpg), [검색 결과 없음](dist/qa/audit-empty-search.jpg).
- 같은 생산 코드의 강조 패널을 별도 **480×332** 검증 창에 띄워 파란 테두리, 긴 프로젝트명 말줄임, TTY 배지와 시간 경과 후 해제를 확인했다. [강조 패널](dist/qa/highlight-panel.jpg). 검증 앱은 사용자 데이터베이스·터미널을 읽거나 승인 입력을 보내지 않는다. 실제 Terminal 창 이동과 강조의 연속 동작, 실제 VS Code 알림, 다크 모드·VoiceOver·요청 복사는 이번 UI 검증에 포함하지 않았다. Computer Use의 Terminal 접근 제한을 우회하지 않았다.
- 앱·확장을 **0.2.3**, 앱 빌드 **6**으로 패키징했고 `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app`를 통과했다. 실행 중인 앱을 교체한 후 Terminal **8개 연결**, 활성 Claude/Codex **9개**, 일시정지 해제, 기존 승인 기록 **5건**을 확인했다. 전체 승인 내역 화면을 열어 기존 기록의 내용과 이전 버전 메타데이터 누락 안내를 확인했다. [실제 승인 내역](dist/qa/audit-production.jpg).
- 재실행 전 켜져 있던 자동 승인 6개는 유지됐다. 재실행 후에는 7개가 켜져 있으며, 추가 1개는 데이터베이스에 이미 저장되어 있던 `true` 설정이 복원된 것을 읽기 전용 조회로 확인했다. 이번 작업에서 사용자 승인 스위치를 변경하지 않았다. VS Code 확장은 패키지만 생성했으며 사용자 VS Code에는 설치하지 않았다.

## 0.2.2 요청 감지·승인 전달 수정

- 실제 앱의 상태 API에서 Terminal 연결이 꺼져 있는데 자동 승인 설정은 유지된 상태를 확인했다. 이전 버전은 Terminal 연결 설정을 저장하지 않았다. 실제 Codex 승인 내역에는 승인 직전 전체 화면 비교가 실패한 `승인 화면이 변경되었습니다. (-2700)`가 남아 있었다. 실패 시 재시도하지 않는 엔진 동작을 확인했다. 해당 실제 화면의 정확한 변경 원인이 한글 정규화인지 로그 변경인지는 확인하지 못했다.
- 명시적으로 켠 Terminal 연결을 저장하고 재실행 후 복원한다. 연결 해제는 유지하고, 일반 연결 오류는 5초 이후 재시도하며, macOS 자동화 권한 거부는 사용자의 재연결 전까지 반복 호출하지 않는다. 이전 버전에서 저장되지 않은 연결은 임의로 켜지 않는다.
- 긴 명령의 승인 창과 한국어 NFC/NFD 차이를 처리한다. Codex는 현재 요청 전체를 비교하여 위쪽 로그 변화에 영향을 받지 않도록 했으며, Claude는 승인 제목 이전의 명령도 비교한다. 선택지·명령·뒤에 추가된 입력창 변경은 입력을 차단한다.
- 입력이 전혀 전달되지 않았다는 어댑터 응답이 있을 때만 다음 화면 수신 후 최대 3회 재시도한다. 성공했거나 전송 여부를 알 수 없는 입력은 반복하지 않는다. 연속 요청 사이의 화면 변화를 관찰하지 못하면 다음 요청도 직접 확인이 필요할 수 있다.
- Claude `AskUserQuestion`과 계획 확인의 내용을 보존하고, 지원하는 선택형 질문을 두 도구 모두 `응답 필요`로 표시한다. 일반 질문에는 자동으로 답을 고르지 않는다. 훅에서 터미널로 반환된 권한 요청은 지원하는 화면 연결이 있을 때 처리하며 이미 훅으로 승인한 요청에 화면 입력을 중복 전송하지 않는다.
- Release 빌드 및 Swift 검사 **23/23**, VS Code TypeScript·화면 모듈 **5/5**, 훅/소켓 통합 검사, 테스트 PTY 통합 검사 통과. 추가 범위: 연결 복원·해제·권한 거부, 긴 명령, 한국어 정규화, 실제 JXA 스크립트의 메모리 모의 실행, 질문 내용·분류, 입력 미전달 재시도와 횟수 제한, 훅 권한 요청의 화면 전환. 사용자 터미널에 테스트 입력을 보내지 않았다.
- 앱과 확장을 **0.2.2**로 패키징했다. `codesign --verify --deep --strict --verbose=2 dist/AutoApprove.app` 통과. 수정본 재실행 후 실제 활성 목록에서 Claude·Codex 9개와 일반 셸 0개를 확인했고 기존 자동 승인 6개 설정이 모두 보존됐다. Claude의 실제 작업 이벤트 수신도 확인했다.
- 기능 테스트와 별도로 실제 앱의 **1040×700**, **784×612**에서 연결 안내·행 상태·선택 전환을 확인하고 연결 설정 시트를 열어 복원 설명과 버튼 상태를 확인했다. 기본 크기로 복원했다. [기본 창](dist/qa/request-fix-desktop.jpg), [최소 창](dist/qa/request-fix-minimum.jpg), [연결 설정](dist/qa/request-fix-settings.jpg). 다크 모드·VoiceOver와 실제 질문 내용의 UI 렌더링은 이번 검증에 포함하지 않았다.
- 사용자가 수정본에서 Terminal 연결을 켠 뒤 로컬 상태 API로 `연결됨 · 8개 세션`, `terminalRequested: true`, `terminalConnected: true`를 확인했다. 전체 활성 목록 9개 중 1개는 Claude의 별도 백그라운드 PTY다. 기존 자동 승인 6개와 전체 일시정지 해제 상태도 유지됐다. 다만 새 승인 기록은 없고 여러 화면 연결 세션이 `unknown`이어서 실제 질문 감지→입력→작업 재개는 아직 확인하지 못했다. 사용자에게 현재 질문·선택지 문구를 요청했다.
- Computer Use 도구의 Terminal 접근 제한은 우회하지 않았다. 실제 Claude/Codex 승인 성공을 테스트 결과나 연결 성공만으로 주장하지 않는다. VS Code 확장 패키지는 생성했지만 사용자 VS Code에 새 버전을 설치하거나 실환경 입력을 검증하지 않았다.

## 0.2.1 Claude Code·Codex만 표시

- 사용자 요구에 맞춰 프로세스 탐색과 엔진 등록을 Claude Code·Codex로 제한했다. 일반 셸과 다른 명령은 목록 및 상태 개수에 포함하지 않는다. 도구 종료 후 셸이 대신 나타나지 않는다. 기존 CLI 입력 대기 감지와 경과 시간은 유지한다.
- 일반 셸 제외, 두 도구의 Terminal/VS Code 탐색, 도구 종료 시 활성 목록 및 대기 수 제거 회귀 검증을 갱신했다. `swift build --disable-sandbox --cache-path .build/cache -c release`, `.build/release/autoapprove-checks` **18/18**, `node scripts/screen-integration-check.mjs .build/release/autoapprove` 통과.
- 앱 0.2.1 실행 후 실제 로컬 상태 API에서 Claude 6개·Codex 2개, 일반 셸 0개를 확인했다. 이전 목록의 일반 셸 34개가 제외됐다. 사용자가 자동 승인을 켠 기존 6개 세션의 설정도 보존됐다.
- 실제 앱의 1040×700 목록과 784×612 대기 필터를 확인했다. 일반 셸이 대기 개수에 섞이지 않고 빈 대기 필터에는 Claude Code·Codex의 입력 대기 설명이 표시된다. 기본 창 크기를 복원했다. 캡처: [에이전트 목록](dist/qa/agents-only-desktop.jpg), [작은 창 대기 필터](dist/qa/agents-only-idle-minimum.jpg).
- 최종 앱의 `codesign --verify --deep --strict` 통과. Terminal 화면 연결은 이전부터 해제된 상태였으며 자동으로 연결하지 않았다. CLI의 화면 기반 대기 감지는 기존 fixture/모의 확장 검증 범위이고, 이번 변경으로 실환경 화면 판정 정확도를 새로 검증한 것은 아니다.

아래 0.2.0의 일반 셸 지원 기록은 이전 동작에 대한 기록이며, 현재 버전에는 적용하지 않는다.

## 0.2.0 대기 터미널 표시

- Claude·Codex 외에 macOS Terminal/VS Code의 일반 셸을 탐색한다. 같은 TTY의 에이전트와 셸은 중복 표시하지 않으며 에이전트 종료 후 셸이 다시 나타난다. 작업 디렉터리 조회는 PID를 모아 한 번에 수행하고 셸의 `cd` 변경을 반영한다.
- 실제 프로세스 탐색에서 42개 활성 세션을 확인했다: 에이전트 8개, 일반 셸 34개. 이 중 일반 셸 30개는 대기, 4개는 명령 실행 중으로 판정됐다. 프로세스 탐색은 Terminal 화면에 접근하거나 입력을 보내지 않았다.
- 일반 셸의 포그라운드/자식 명령, Claude의 Stop·idle_prompt·UserPromptSubmit 이벤트, 지원하는 CLI 입력창·중단 안내를 사용한다. 화면 대기는 동일한 화면과 연결 세대를 2초 이상 재확인한다. 조용한 작업, 백그라운드 자식 명령, 알 수 없는 화면, 연결 해제는 대기로 오인하지 않는 fixture를 검증했다. 일반 셸은 승인 감지·전송에서 제외했다.
- 핵심 검사 **18/18 통과**. 일반 셸/명령/백그라운드 작업, 에이전트 종료 후 셸 복귀, 입력창/작업/질문/미확인 분류, 화면 세대 변경, 대기 시간 유지·초기화, 훅 이벤트 전환, 재실행 직후 훅이 먼저 도착하는 설정 보존 회귀를 추가했다. 최종 release 검사도 통과했다.
- VS Code TypeScript 빌드 및 화면 모듈 **3/3 통과**. 확장은 관찰한 셸 명령 실행 여부를 등록 메시지에 추가한다. 이미 실행 중인 명령의 과거 출력을 복구하지 않는다. 실제 VS Code 확장 UI의 명령 이벤트 왕복은 이번 검증 범위에 포함하지 않았다.
- 훅/소켓 통합 검사 통과. 테스트 PTY와 모의 확장을 사용하는 화면 통합 검사에서 입력 대기 1회 관찰은 미확인, 반복 관찰 후 대기, 새 작업 시 작업 중 및 대기 시각 제거, 대기 감지 중 승인 입력 없음도 확인했다. 사용자 터미널에는 테스트 입력을 보내지 않았다.
- 실제 앱에서 1040×700, 784×700, 최소 784×612를 확인했다. 42개 밀집 목록, 대기 필터, 대기 감지 후 경과 시간, TTY 검색, 긴 프로젝트 이름, 검색 결과 없음, 검색 지우기, 목록 스크롤, 일반 셸의 승인 스위치 제외, 연결 전 VS Code 이동 버튼 비활성을 확인했다. 기본 창 크기로 복원했다. 캡처: [최소 창 대기 목록](dist/qa/idle-minimum.jpg), [최종 기본 창 목록](dist/qa/idle-desktop.jpg).
- UI QA 중 재실행 직후 훅이 먼저 도착하면 저장된 자동 승인 설정을 복원하지 못하는 문제를 발견해 수정했다. 최종 앱의 로컬 상태 API에서 사용자가 켠 기존 6개 세션 모두 `automatic: true`가 유지됨을 확인했다.
- 앱 및 확장 패키지 0.2.0을 생성했다. 최종 앱의 `codesign --verify --deep --strict` 통과. 기존 Terminal 직접 접근 제한은 유지되며 우회하지 않았다. 앱 재실행 후 Terminal 화면 연결은 사용자가 연결 설정에서 다시 켜야 한다. Claude 훅은 실제 작업 이벤트 수신을 확인했다. Claude·Codex 화면 기반 대기 분류는 fixture/모의 확장으로 검증했으며, 사용자 Terminal의 실제 화면 판정 정확도를 검증했다고 주장하지 않는다.

대기 판정의 한계: 셸 내부 내장 명령, 사용자 정의 CLI 화면, 완료 훅 이후 다른 훅이 이어가는 작업은 추론의 한계가 있다. 대기 시간은 감지 이후부터 계산한다. 앱 재시작 전 대기 시간은 복원하지 않는다. 다크 모드와 VoiceOver 전체 흐름은 이번에도 확인하지 않았다.

## 0.1.1 연결·누락 수정

- 사용자 환경에서 앱 상태의 `Terminal 연결 실패 … (-1728)`를 확인했다. 설치된 `Terminal.sdef`에는 탭의 `name` 속성이 없다. 사용하지 않던 제목 조회를 제거하고 `tty`·`contents`만 읽도록 수정했다.
- `ps`의 마지막 열 앞 정렬 공백이 남아, 절대 경로 없이 `claude`·`codex`로 표시된 실행 파일을 인식하지 못했다. 마지막 열의 정렬 공백을 제거했다. 실제 프로세스 비교에서 누락됐던 Claude 4개가 복구됐고 수정 직후 8개 CLI 세션을 탐색했다.
- 조상 Terminal과 다른 PTY에서 실행되는 백그라운드 에이전트를 같은 Terminal 탭으로 잘못 분류하지 않도록 수정했다. 해당 프로세스는 목록에 남기되 터미널 종류는 미확인으로 표시한다.
- 닫힌 창·탭은 개별 실패로 기록해 다른 탭을 계속 읽고, 읽기에 실패한 세션의 기존 화면 연결·예약 입력은 폐기한다. 권한 거부는 성공으로 숨기지 않는다.
- 새 회귀 검증 3개를 포함한 핵심 검사 11/11, 기존 훅·소켓 및 PTY 통합 검사 통과. Terminal JavaScript 자동 검증은 JavaScriptCore의 모의 객체로 수행했고 테스트 코드에서 실제 Apple event를 보내지 않았다.
- release 앱을 0.1.1로 다시 패키징해 코드 서명을 확인했다. 실제 1040×700 관리 창에서 8개 세션과 각 TTY·PID를 확인하고, `ttys030` 검색이 기존 누락 세션 하나를 반환하는 동작을 확인했다. 화면은 `dist/qa/fixed-session-list.jpeg`에 저장했다.
- 사용자가 수정본에서 재연결한 뒤 앱의 로컬 상태 API로 `연결됨 · 7개 세션`과 7개 세션의 `terminalScreen` 채널을 확인했다. 전체 탐색은 8개이며 나머지 1개는 Claude가 만든 별도 백그라운드 PTY여서 `unknown`으로 구분됐다. 사용자 Terminal에 모델이 직접 입력하지 않았으며, 실제 권한 요청의 자동 승인 후 작업 재개 검증은 이번 연결 확인과 별개다.

아래는 0.1 최초 구현 시의 검증 기록이다.

## 기능 검증

| 검증 | 결과 | 범위 |
|---|---|---|
| Swift 핵심 검증 | 8/8 통과 | 권한 창 감지·오탐 fixture, 선택 옵션, PID+시작 시각 식별, 자식 프로세스 제외, 전경 그룹, 기본 비활성, 일시정지, 중복·세션 분리, 설정 보존·SQLite, 끊긴 연결 OFF·이동 가능 여부 |
| VS Code 화면 모듈 | 3/3 통과 | xterm 커서·지우기·대체 화면, 화면 해시·세대·만료·단일 사용, 미반영 출력이 있을 때 입력 차단 |
| 훅/소켓 통합 | 통과 | 실제 helper stdin → Unix socket → 승인 JSON, 조각난 메시지, 비활성·일시정지·중복 방지, 감사 기록·CLI status |
| PTY 화면 승인 통합 | 통과 | 임시 PTY 3개에 테스트 전용 실행 파일, 프로세스 발견·전경 검사, 정확한 대상, 동시 켜기, 같은 화면 한 번만 전송, 재개, 화면→훅 전환 시 예약 취소, 끊긴 세션 끄기 |
| 언어별 식별 | 통과 | 한국어·영어 환경의 실제 프로세스 ID가 같음. `ps` 실행에 C locale 고정 |
| 실제 세션 탐색 | 확인 | 현재 실행 중인 Claude/Codex의 PID·TTY·경로를 탐색. 자동 승인 기본 OFF 확인 |

테스트용 PTY는 직접 컴파일한 입력 대기 프로세스다. 가짜 VS Code 연결이 승인 메시지의 대상을 확인하고 응답한다. 실제 Claude/Codex 모델 호출, 실제 VS Code 터미널 입력, Terminal 자동화 권한을 검증한 결과로 해석하면 안 된다.

전체 Xcode가 없어 `swift test`의 XCTest 모듈을 사용할 수 없는 환경이므로, 동일한 핵심 검증을 Foundation 기반 `autoapprove-checks` 실행 파일로 제공한다.

## 실제 화면 확인

`computer-use`로 패키징한 네이티브 앱을 실행해 기본 크기 1040×700, 좁은 창 784×700, 최소 높이의 작은 창 약 784×612에서 라이트 화면을 확인했다.

- 실제 세션 목록, 연결 전 비활성 스위치, 세션 변경에 따른 상세 정보 전환.
- 검색 입력·지우기, 검색 결과 없음 화면.
- 자동 승인 필터 및 빈 결과.
- 연결 설정 시트, 각 연결 설명·설치 버튼, 스크롤 영역.
- 전체 일시정지 안내와 재개 후 상태 복원.

시각 검증은 빌드·기능 테스트와 별도로 실시했다. 다크 모드, VoiceOver 전체 흐름, 많은 승인 내역·긴 명령의 실제 렌더링은 아직 검증하지 않았다. 모바일·태블릿·웹 인터페이스는 이 앱의 대상이 아니다. 이번 실행의 화면 캡처는 `dist/qa/`에 저장했다.

읽기 전용 마무리 코드 검토에서 발견한 예약 취소·다중 켜기·끊긴 연결 제어·기존 훅 요청 안내·터미널 이동 가능 여부·메뉴 응답 수는 수정했고 재검토했다.

## 실제 CLI 연결에서 남은 검증

| 조합 | 탐색·구현 상태 | 실제 승인창의 입력 후 작업 재개 |
|---|---|---|
| Terminal + Claude | 실제 프로세스 탐색 확인, 화면 어댑터·훅 helper 구현 | 미검증 |
| Terminal + Codex | 실제 프로세스 탐색 확인, 화면 어댑터 구현 | 미검증 |
| VS Code + Claude | 프로세스 fixture·확장·훅 구현 | 미검증 |
| VS Code + Codex | 프로세스 fixture·확장 구현 | 미검증 |

이번 환경의 Computer Use 도구가 `com.apple.Terminal` 접근을 거부했다. 다른 경로로 실제 Terminal UI 접근을 우회하지 않았으며, Terminal 어댑터의 실환경 승인 성공을 주장하지 않는다. Claude 전역 훅과 사용자의 VS Code 확장을 자동 설치하지 않았고, 기존 사용자 세션에 승인 입력을 보내지 않았다.

VS Code의 안정 API는 확장이 `read()`를 호출한 이후의 출력만 제공한다. 이미 실행 중인 명령의 출력을 소급해서 연결할 수 없으므로 앱과 확장을 연결한 뒤 CLI를 다시 실행·이어하기 해야 한다. Claude 훅은 기존 세션의 설정 재적용 시점도 추가 확인이 필요하다.

Codex의 기존 공유 app-server 소켓은 이 환경에서 발견하지 못했다. 이번 버전은 app-server 연결을 제공하지 않는다. Terminal 화면의 재확인과 입력은 원자적이지 않으며, 일시정지는 이미 전달을 시작한 입력을 취소하지 않는다.

실제 설치 환경에서 권한 요청 종류·CLI 버전별 화면 호환성, 앱/확장 재시작, 잠자기·복귀, 10개 동시 세션과 지연 시간 측정은 후속 검증 항목이다.

## 재현 명령

```sh
swift build --disable-sandbox --cache-path .build/cache
.build/debug/autoapprove-checks
npm --prefix extensions/vscode test
node scripts/integration-check.mjs
node scripts/screen-integration-check.mjs
bash scripts/build.sh
```

최종 release에서도 `.build/release/autoapprove-checks`, `node scripts/integration-check.mjs .build/release/autoapprove`, `node scripts/screen-integration-check.mjs .build/release/autoapprove`가 통과했다. 전체 빌드 스크립트로 앱·확장 패키지 생성 및 로컬 ad-hoc 서명을 확인했다. 최종 패키지의 `codesign --verify --deep --strict`와 내장 CLI 실행도 통과했고 앱을 다시 열어 기본 OFF 상태를 확인했다.

로컬 소켓·프로세스·PTY 접근을 차단하는 실행 샌드박스에서는 통합 테스트를 정상적인 로컬 개발 환경에서 실행해야 한다. 테스트는 임시 데이터와 테스트 프로세스를 종료·정리한다.
